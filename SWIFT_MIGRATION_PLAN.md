# SWIFT_MIGRATION_PLAN

Перенос Light Plan с PWA на нативный Swift / SwiftUI.

**Статус.** Phase 0 (reverse engineering) закрыта. Архитектура предложена, но
намеренно **не закрыта**: два её вопроса решаются прототипом, а не документом.
Реализация: собран скелет проекта (итерация 2), логики в нём нет.

**19 сентября 2026: итерация 1 сделана черновиком** —
[`docs/17_NATIVE_ARCHITECTURE.md`](docs/17_NATIVE_ARCHITECTURE.md). Разделы A–D
ниже — слепок 26 августа: с тех пор `beta/index.html` вырос с 19 428 до 36 900
строк, и что это меняет в итерациях, сказано в § 22 документа.

**19 сентября 2026: итерация 2 сделана** — скелет в `light_plan:native/`,
коммит `ea44883` в его локальном git. Что показал замер, см. итерацию 2 ниже.

**Редакция вторая.** Первая была построена по обычной логике «изучил код →
разложил по технологиям → пошёл по фазам». Она содержала три ошибки, найденные
разбором самого задания, и здесь они исправлены:

1. источником правды был назначен код, тогда как в этом проекте правда живёт в
   `DECISIONS.md` и `docs/`;
2. поведение делилось на два вида — «переносим» и «улучшаем потом», — а видов
   три: часть веб-поведения не замысел, а обход ограничения браузера;
3. прототипы двух самых рискованных мест стояли в середине плана, после
   двенадцати итераций исполнения. Теперь они идут сразу после скелета.

**Номера строк — снимок на коммит `c0fbbbf` (26 августа 2026).** Веб правится
дальше, и они дрейфуют: за время написания этого документа файл вырос на 17
строк, и всё после солнечной модели уехало на 18. Искать нужный блок следует по
якорному заголовку в комментарии (`СОЛНЕЧНАЯ МОДЕЛЬ`, `ПЕРЕДАЧА РАЗРЯДА`,
`КАРТОЧКА СОБЫТИЯ`), а номер держать за подсказку, а не за адрес. То же правило
у `Tools/parity/extract.js`: он режет по якорям, не по числам.

---

## Порядок чтения источников

Не «сначала код». Порядок такой:

1. **`docs/01…12`** — что за продукт и почему он такой. Неизменяемая часть.
2. **`DECISIONS.md`** (773 КБ) — почему сделано именно так и что было
   отвергнуто. Половина кода написана вторым и третьим заходом; первые заходы
   отвергнуты по записанным причинам. Кто читает только код, повторит их.
3. **`ROADMAP.md`**, раздел «Нативная веха» — что веб не может физически и что
   надо доделать на вебе до переезда. Это входное условие миграции.
4. **`beta/index.html`** — слепок решений. Читается последним и как
   свидетельство, а не как замысел.

Правило на всю миграцию: **если код и `docs/` расходятся — это не задача
исполнителя.** Расхождение выносится наверх как продуктовый вопрос.

**Источник правды по коду:** `beta/index.html` (19 428 строк). Бета опережает
корень, правится она. Все номера строк ниже — по бете.

---

## 0. Что уже лежит на диске

| Где | Что | Роль |
| --- | --- | --- |
| `Light_Plan/beta/index.html` | приложение целиком | донор поведения |
| `Light_Plan/beta/lang.js` | словарь, 207 КБ. `ru`, `en`, `zh` заполнены (~1 140 ключей), `es` и `ja` — заготовки с правилами числа; есть варианты `en-US` и `en-GB` | донор строк |
| `Light_Plan/beta/icons.js` | библиотека знаков (SVG), 66 КБ | донор графики |
| `Light_Plan/beta/qr.js` | генератор QR для опросника | донор |
| `Light_Plan/quest/wedding.html` | опросник клиенту, отдельная страница | донор |
| `Light_Plan/docs/01…12` | продуктовая конституция | ТЗ |
| `Light_Plan/DECISIONS.md` | причины и отвергнутое | ТЗ и защита от повторов |
| `Light_Plan/ROADMAP.md` | «Нативная веха» | входное условие |
| `Light_Plan/tools/` | `shot.js`, `datecheck.js`, `langcheck.js` | прототипы стенда паритета |
| `~/Documents/LightPlan/` | пустой шаблон Xcode | не переиспользуется |
| `~/Documents/Event_OS/` | платформа ролей, Light Plan = Organizer | горизонт |

**Обёртки WKWebView на диске нет.** `~/Documents/LightPlan/LightPlan.xcodeproj`
— нетронутый шаблон мастера (`Item.swift`, `ContentView` со SwiftData,
iOS/macOS 26.5, Swift 5 mode). Нативного кода Light Plan сегодня не существует.

---

## A. Архитектура существующего Light Plan

### A.1 Физическая структура

Один файл — всё приложение. Сборки нет, зависимостей в продукте нет.

```
beta/index.html   19 428 строк
├─ 1…25      <head>: мета, манифест, Leaflet 1.9.4 с CDN
├─ 26…880    <script> — слепок icons.js, вклеенный руками
├─ 881…882   <script src=lang.js?v=5>, <script src=qr.js?v=1>
├─ 883…4714  <style> — 3 832 строки, ~1 200 правил, палитра токенами
├─ 4716…6617 <body> — 1 902 строки, 623 идентификатора
└─ 6618…19425 <script> — 12 808 строк, 639 функций, 316 слушателей
```

Одна IIFE, ~200 глобальных переменных, доступ к DOM через `$("id")` —
553 разных идентификатора. Модулей, классов, импортов нет.

### A.2 Логические слои

```
Light Plan
├── ВЫЧИСЛЕНИЯ (чистые, без DOM)
│   ├── Солнце NOAA              6810…6877
│   ├── Луна (Меёс, низкая точность) 6878…6917
│   ├── Млечный Путь             6918…7129
│   ├── Затмения таблицей        7130…7244
│   ├── Погодный мок             7245…7312
│   ├── Open-Meteo и закатный балл 7313…7441
│   ├── Состояние света          7442…7574
│   └── Звёзды и метеоры         7575…7649
├── СОСТОЯНИЕ                    7650…7719
├── ИНФРАСТРУКТУРА
│   ├── Облако: три поставщика   7720…8307
│   ├── Геокодер                 10221…10310
│   ├── Звук и отдача            17116…17338
├── ДОМЕН
│   ├── Жанры, занятое время, точки, организации, деньги, сдача 8308…8960
│   ├── Наложения                13953…14124
│   └── Фаза события, свет-условие, порядок блоков 14229…14375
└── UI
    ├── «Свет»                   8961…9254
    ├── «Карта»                  9255…10376
    ├── «Съёмки»                 10377…11812
    ├── Шторка барабанов         11813…11950
    ├── Форма                    11951…12350, 13344…13952
    ├── Организации и документы  12351…12435
    ├── Мудборды                 12436…13343
    ├── Карточка съёмки          14125…14201
    ├── Карточка события         14202…16458
    ├── Пересчёт и навигация      16459…16574
    ├── Лента времени            16575…16949
    ├── Передача разряда         16950…17115
    ├── Настройки и знакомство   17339…17973
    ├── Купол, слои, компас, вееры 17974…19350
    └── Старт                    19351…19425
```

### A.3 Вес работы — измеренный, а не предполагаемый

Главная поправка к первой редакции. Технологический список фаз («солнце,
локация, погода, таймлайн, экраны») подразумевает примерно равный вес. Он не
равный:

| Блок | Строк | Доля кода | Что это значит для плана |
| --- | ---: | ---: | --- |
| Карточка события | 2 257 | 17,6 % | крупнее всей математики неба вместе взятой |
| Планировщик (5 видов) | 1 436 | 11,2 % | |
| Купол, слои, компас, вееры | 1 377 | 10,8 % | |
| Карта | 1 122 | 8,8 % | |
| Мудборды | 908 | 7,1 % | в первой редакции почти не был назван |
| Форма (два блока) | 1 009 | 7,9 % | |
| Домен: жанры, деньги, сдача | 653 | 5,1 % | |
| Настройки и знакомство | 635 | 5,0 % | |
| Облако | 588 | 4,6 % | |
| Лента и передача разряда | 541 | 4,2 % | самый рискованный, не самый крупный |
| Звук и отдача | 223 | 1,7 % | |
| Наложения | 172 | 1,3 % | |
| **Вся математика неба и света** | **841** | **6,6 %** | **переносится формула в формулу** |

Вывод, который меняет план: **астрономия — самая лёгкая часть миграции**, у неё
есть эталон и фикстуры. Тяжесть в карточке, планировщике и форме — там объём, но
там же и написанная спецификация, то есть работа исполнителя. Риск сосредоточен
в третьем месте: 541 строка ленты и передачи разряда.

### A.4 Три корзины, а не две

Вторая поправка. Делить поведение на «переносим сейчас» и «улучшаем потом»
недостаточно: часть веб-поведения — не замысел, а обход ограничения браузера.
Перенести его дословно значит закрепить чужое ограничение как своё решение.

**Корзина 1 — переносится как есть.** Замысел, проверенный и записанный.

- сутки от полуночи до полуночи (6826), плоский диск светила без ореола,
  дуга не продолжается вниз, взвод и срыв с их константами, состав и порядок
  блоков карточки, матрица полей по жанрам, коды состояний света.

**Корзина 2 — паритет замысла, реализация другая.** Поведение сохраняем,
механику обхода выбрасываем. Каждый пункт — с указанием, что именно меняется.

| Что в вебе | Почему так | Что в Swift |
| --- | --- | --- |
| `computeSun` мутирует глобальные `SUN`/`decl`/`solarNoon`, чужой день считают приёмом «сохранить → посчитать → вернуть» (16616, 7380) | нет типов | `SolarDay` — значение; приём исчезает вместе с классом ошибок |
| `windSpent` + `releasePointerCapture` (16950…17115) | нативный `range` сам пересчитывает значение из позиции пальца | свой ползунок на `DragGesture`; **поведение то же, обход не нужен** |
| Отдача через физический `<input type=checkbox switch>` под пальцем (17240) | квирк iOS, единственный доступный вебу | `.sensoryFeedback`; **снимаются три записанных ограничения** — появляются разные тики, отдача на событие и отдача на детентах |
| Тень светила — `<circle>` с радиальным градиентом | CSS-фильтру на SVG в Safari нельзя доверять (подвёл дважды) | тень как свойство слоя |
| `fitTitle` — подбор кегля перебором (14686) | нет автоподгонки | `minimumScaleFactor` |
| `measureDock`, `--tabh`, `--dockh`, `visualViewport` (19351) | Safari двигает панель | safe area |
| `CHANNEL` beta/main, разведение ключей `localStorage` (7676) | бета и релиз на одном origin | разные сборки |
| Слепок `icons.js`, вклеенный в `index.html` руками | «одним файлом» | ресурсы, генерируются |
| Nominatim + BigDataCloud двумя поставщиками | нативного геокодера нет | `CLGeocoder` |
| `localStorage` как хранилище | другого нет | файловая система |

**Отдельно: мок погоды — это корзина 1, а не 2.** Он детерминированный и умеет
молчать: барабан не рисует погодный знак там, где нет настоящего прогноза
(16666). Переносится, но `isReal` становится полем типа, чтобы правило «прибор
не врёт» держалось компилятором, а не дисциплиной.

**Корзина 3 — улучшения.** Всё, что заметно по дороге, но не делается во время
миграции. Список в разделе «Potential Native Improvements».

### A.5 Зависимости наружу

| Зависимость | Где | Чем становится |
| --- | --- | --- |
| Leaflet 1.9.4 (CDN) | карта | MapKit — но см. итерацию 4 |
| CARTO tiles | тёмный холст | открытый вопрос, решается прототипом |
| Open-Meteo | погода, 16 дней вперёд, 5 назад | остаётся (§ 4.6) |
| Nominatim / BigDataCloud | имя места | `CLGeocoder` |
| Яндекс.Диск / Dropbox / Google Drive | копия, документы, референсы | остаются, роль меняется |
| Playwright | `tools/shot.js` | остаётся стендом сверки |

В продукте npm-зависимостей нет. `package.json` держит только Playwright.

---

## B. Карта функций

Критичность: **1** — без этого продукта нет; **2** — заметная потеря;
**3** — удобство. Корзина — по § A.4.

### B.1 Свет и небо — 841 строка

| Функция | Где | Крит. | Корзина |
| --- | --- | --- | --- |
| `computeSun`, `elevAt`, `azAt`, `tAtElev` | 6815…6866 | 1 | 2 (значение вместо глобалов) |
| `shadowAt`, `shadowWord` | 6869, 7552 | 1 | 1 |
| `stateAt` — 15 состояний | 7521…7551 | 1 | 1 |
| `nextLight` | 7563 | 1 | 1 |
| Полярный день и ночь (`SUN.polar`) | 6842 | 2 | 1 |
| `moonAt`, `moonEq`, фаза | 6878…6917 | 2 | 1 |
| Млечный Путь: полоса, ядро, полуширина | 6918…7129 | 2 | 1 |
| Затмения таблицей (расчёт отвергнут трижды) | 7130…7244 | 2 | 1 |
| `sunsetScore` по трём ярусам и влажности | 7326 | 1 | 1 |
| `qualityOf`, `dayWeather`, мок | 7276…7311 | 1 | 1 + поле `isReal` |
| Звёзды, метеоры | 7575…7649 | 3 | 2 (Canvas вместо DOM) |

### B.2 Планировщик — около 6 500 строк

| Функция | Где | Крит. | Корзина |
| --- | --- | --- | --- |
| Запись съёмки (сборка `rec`) | 16389…16433 | 1 | 2 — правка должна менять запись, а не собирать новую |
| Жанры, группы, пресеты, папки | 8325, 13344…13530 | 1 | 1 |
| Наложения в дне | 13953…14124 | 1 | 1 |
| Фаза события до/во время/после | 14243 | 1 | 1 |
| Маршрут дня (точки) | 14153…14201 | 1 | 1 |
| Сроки сдачи, статистика | 8799…8867, 11145 | 2 | 1 |
| Деньги: ставка, расходы, предоплата, валюта | 8713…8798 | 2 | 2 — `Decimal` вместо `Number` |
| Организации и документы | 12351…12435 | 2 | 1 |
| Мудборды (`tag` на референсах) | 12436…13343 | 2 | 1 |
| Занятое время (`blocks`) | 8587…8623 | 2 | 1 |
| Корзина и отмена удаления | 11686…11812 | 2 | 1 |
| Опросник клиенту (QR, разбор ответа) | 16048…16334 | 2 | 2 — ссылка и разбор остаются, страница отдельно |
| Поиск, сигнал перегруза | 18451, 11208 | 3 | 1 |

### B.3 Инфраструктура

| Функция | Где | Крит. | Корзина |
| --- | --- | --- | --- |
| `saveAll` / `loadAll` — весь снимок одним JSON | 7678, 8288 | 1 | 2 |
| **Семь миграций данных** | 8325…8460 | 1 | 1 — обязательны, иначе живые записи не откроются |
| Копия в файл и восстановление | 8243…8307 | 1 | 2 — становится полным архивом |
| Облако: три поставщика, девять обязанностей | 7720…8307 | 2 | 1 по интерфейсу, 2 по реализации |
| Open-Meteo, дебаунс, тихий откат | 7313…7441 | 1 | 1 |
| Геокодер, чистка имени | 10221…10310 | 2 | 2 |
| Звук | 17160…17240 | 3 | 2 |
| Отдача | 17221…17300 | 3 | 2 — ограничения снимаются |
| Уведомления | 16335 | 2 | смена прогноза и наложения работают в вебе баннером; расписание делается впервые |

---

## C. Карта данных

### C.1 Как есть в коде

```
Настройки (одна запись, ключ lightplan.beta.v1 / lightplan.v1)
├── loc {lat, lon} — место, к которому привязан весь свет
├── theme, clock, tempUnit, currency, timeStep, travelMin
├── pro, ribbonMode, drumSlot, mapLayers, mapLabels, mapFold, dayFold
├── practice, genres[], genrePrefs{}, defaultRate, delivery{mode,days}
└── equipment[], packs{}, cardOrder{}, sync

Session (sessions[])
├── id, kind (съёмка | встреча), date, min, end, dur, type
├── contact, clientTel, persons[{n,tel}], orgId, person, phone
├── place, placeLat, placeLon, placeCity
├── wish[], warn, route[{t,n,…}]
├── deadlineChoice ("auto" | дни | null), delivered, deliveredAt
├── pay, rate, units, expense, prepay, currency
├── guests, trip, tripPlace, brief, models, notes, gear[]
├── docs[{k,path,name,size,url,kind}], refs[{k,path,…,tag}]
├── questSent, fromMeetOn/fromMeetId/grewOn/grewToId
└── synced

Org (orgs[]) · Block (blocks[]) · Spot (spots[]) · Trashed (trashed[]) · genreRefs{}
```

Не хранятся, считаются: `Moment`, `SolarDay`, `WeatherDay`, `LightState`,
`Overlap`, `Phase`, срочность сдачи. В Swift должны считаться так же.

### C.2 Целевая модель

Имена из `docs/05_INFORMATION_ARCHITECTURE.md` — модель там описана давно, код
просто не выделил её в типы.

```
Value types, Codable, Sendable

Place        lat, lon, name, city, tz
SolarDay     date + place → все времена и высоты, детерминирован
MoonSample   alt, az, dist, phase
SkyBand      полоса Млечного Пути на момент
LightState   code, level, tone, colour, glow            15 кодов
WeatherDay   quality, cloud, temp, wind, trend, sunsetScore, layers, isReal
Moment       date + minute + place → всё выше разом     ← «главная сущность»

Session, RoutePoint, Org, Block, Spot, TrashedItem, GenreRefs, Settings
enum Genre, GenreGroup, PayKind, DocKind, Wish, BlockKind, RefTag
Decimal в деньгах
```

**Правило:** ни один слой ниже UI не знает о SwiftUI, о `Color` и о том, включён
ли астро-режим. Цвет светила — `(r,g,b)` из модели света: она про физику, тема её
не трогает (заголовок палитры в CSS, строка 884).

### C.3 Миграции — самая дорогая часть плана (снято итерацией 12)

В коде было семь разовых миграций: жанр (русское имя → код), пожелание, вид
документа, валюта записи, папка референсов, ключ Яндекса, поля мудборда. Все
лечили один класс ошибки: **строка, которую где-то сравнивают, всегда
оказывается ключом.**

В Swift это снимается `enum(String)`. Первая редакция этого раздела требовала,
чтобы читатель старого JSON понимал и русские имена — иначе первый запуск
нативной версии потерял бы живые съёмки сезона. Это отменено: «Принято:
читается только текущий формат» (`docs/17_NATIVE_ARCHITECTURE.md` § 6, Алексей
19 сентября 2026, «проект PWA закончен» — формат веба окончательный). Из семи
миграций в коде веба осталось две — `migrateCurrency` (снят типом
`Session.currency: Currency?`, итерация 11) и `TAG_CODE` (снят
`RefTag.russianNames`, итерация 11, читает его `shots`/`boards` в итерации 28).
Итерация 12 делается **до** первого экрана планировщика не из-за миграций, а
потому что без хранилища планировщику нечего показывать.

---

## D. Карта UI

### D.1 Четыре экрана

**«Свет» (`s-today`, разметка 4843…4981, отрисовка 8961…9254).**
Шапка (место, дата, погода) → купол 390×240 (дуга дня, светило, тень, зарево,
свет ушедшего светила, звёзды, метеоры, горизонт, кольцо «сейчас», лунный диск с
фазой) → тумблер солнце/луна → показание (время, фаза, ощущение) → «что дальше и
через сколько» + спарклайн облачности → телеметрия семью строками → спойлер
«Подробно» → кнопка съёмки. Планировщику сюда нельзя.

**«Карта» (`s-map`, 4982…5102, отрисовка 9255…10376).**
Leaflet под своим SVG-прибором. Ротор поворачивает карту и прибор одним слоем,
размер ротора — диагональ кадра (9504). Слои: солнце, луна, Млечный Путь. Ночная
вуаль по высоте солнца до −18°. Прибор: лимб латунью, деления, восемь чисел
азимута в астро, две оси, компас-роза, пояс рабочей высоты, путь солнца с
часовыми засечками, луна, три яруса точек МП, трек ядра, чип азимута.
Сводка снизу сворачивается, состояние переживает перезапуск.

**«Съёмки» (`s-plan`, 5103…5223, отрисовка 10377…11812).**
Пять видов: месяц, неделя (семь световых лент), день, год, год-12 с зумом в
месяц. Закреплённая неделя дат, сводка дня, лента дня с часовой осью,
свободными окнами (`openGaps`), съёмками и занятым временем. Три действия:
съёмка, встреча, занять время. Статистика сроков, прибыль по валютам, сигнал
перегруза, корзина.

**«Настройки» (`s-set`, 5224…5486).**
22 раздела, сгруппированных в семь глав (коммит `4aa0a95`, 26 августа 2026):
режим, вид прорези барабана, тема, шаг времени, слои и подписи карты, хранилище
→ мудборды, организации, корзина, знакомство, порог дороги, копия данных,
облако, язык/единицы/валюта, практика ведения дел, обратная связь, номер сборки.
Группировка новая и в нативной версии переносится как есть: пункты не
переезжали, менялись только главы.

### D.2 Оверлеи и листы

10 оверлеев: карточка, форма, год, год-12, статистика, поиск, организации,
карточка организации, мудборды, папка мудборда.
9 листов: выбор момента (барабаны), материал мудборда, занять время, корзина,
практика, место, мои жанры, оборудование, опросник.

**Карточка события** (14202…16458) — самостоятельная подсистема: три вида по
фазе, 14 переставляемых блоков с памятью порядка по группе жанров, стопка
карточек дня, лента точек со знаками, плитка дня, студийный час, полноэкранные
референсы, цепочка сделки, режим перестановки драгом.

### D.3 Таймбар — общий орган «Света» и «Карты»

```
 ┌ восход ─── ↺ сейчас ─── закат ┐   строка краёв
 │  лента / барабан (только астро) │  контекст соседних суток
 │  риски + дорожка света + ползунок│ strain-lane
 └─────────────────────────────────┘
```

Слайдер привязан к одним суткам (`MINT…MAXT`); через сутки переходит лента — или
срыв разряда на упоре. Два вида ленты: `lane` (трое суток шириной в экран) и
`drum` (ячейки по 50 px со знаком, следует за `skyMode`). Дорожка света —
градиент по высоте светила.

### D.4 Жесты

| Жест | Где | Смысл |
| --- | --- | --- |
| Ведение ползунка | таймбар | время суток |
| Ведение за упор + удержание | таймбар | взвод → срыв → соседние сутки |
| Драг ленты и барабана | таймбар | сутки, с пружиной и осадкой |
| Колесо, стрелки | лента | шаг по суткам |
| Щипок | карта | масштаб |
| Драг | карта | место |
| Тап по луне | карта | азимут и высота |
| Свайп влево | строка съёмки | удалить в корзину |
| Потянуть стопку | карточки дня | разложить |
| Драг блока | карточка, перестановка | порядок блоков |
| Драг барабанов | шторка | дата, час, минута |
| Тап-зум | год-12 → месяц | переход с увеличением |
| Спойлеры и фолды | везде | прогрессивное раскрытие |

---

## LightPlan Core UX

Инварианты. Выведены из `docs/`, `DECISIONS.md` и кода — не придуманы. Проверка
против этого списка входит в Definition of Done каждой итерации. Нарушение
требует записи в `DECISIONS.md`, а не молчаливого «в SwiftUI так удобнее».

1. **Время — главный навигационный слой.** Не календарь и не карта.
2. **Один момент на всё приложение.** `viewMin` и `selDate` общие для «Света» и
   «Карты»; переход между экранами момент не сбрасывает.
3. **Сутки всегда от полуночи до полуночи.** Сдвиг начала суток под звёзды
   пробовали и забраковали: шкала, меняющая начало вместе со слоем, ломает
   сверку по памяти (6826).
4. **Через сутки переходит механизм, а не прыжок.** Взвод и срыв — видимая
   передача разряда, как в одометре.
5. **Лента едет, метка стоит.** При перецентровке метка не сдвигается ни на
   пиксель.
6. **Купол — прибор, а не иллюстрация.** Диск плоский, с обозначенной кромкой;
   растушёванный ореол отвергнут. Дуга вниз не продолжается.
7. **Цвет светила — физика, а не оформление.** Тема меняет интерфейс, но не
   модель света. Читаемость решается глубиной, а не зажатием цвета.
8. **Светлая тема — не инверсия тёмной.** Блик белый и тень тёмная в обеих;
   инвертируются только чернила.
9. **Свет — условие, а не раздел.** В карточке появляется, только когда пресет
   требует, точка дня попадает в окно и есть что сказать.
10. **Погода — модификатор света, а не свой экран.**
11. **Прибор не врёт.** Где нет настоящего прогноза — пусто, а не правдоподобно.
12. **Решение раньше данных.** Решение → свет → время → место → объяснение →
    детали → исходные данные.
13. **Планировщик не протекает на экран света.**
14. **Каждая возможность умеет исчезнуть.** Режим, жанр, уровень. Ни один не
    спрашивает пользователя, кто он такой.
15. **Никогда не просить дозаполнить карточку.** Пустое поле — не долг.
16. **Предупреждаем, но не запрещаем.** Наложение — факт, а не ошибка ввода.
17. **Удаление обратимо.** Корзина и полоса отмены.
18. **Код внутри, имя на экране.** Перевод не ломает данные.
19. **Данные живут на устройстве.** Сервера нет; копия уходит в личное облако.
20. **Открывается там, где работают.** Первый запуск — «Свет», дальше «Съёмки».
21. **Собственный язык продукта.** Латунь, чернила, гравировка, одна толщина
    линии; узнаваемость без логотипа.

---

## 4. Нативная архитектура

Каждое решение с доводом. Технология без довода не берётся. Два вопроса
намеренно оставлены открытыми до прототипов (итерации 3 и 4).

### 4.1 Форма проекта

```
LightPlan/
├── App-iOS/  App-macOS/         тонкие таргеты: точка входа и Scene
├── Packages/
│   ├── LightPlanCore            только Foundation. Математика неба и света.
│   ├── LightPlanDomain          только Foundation. Сущности и правила.
│   ├── LightPlanTimeline        только Foundation. Время и механика разряда.
│   ├── LightPlanData            + сеть и файлы. Хранилище, погода, гео, облако.
│   └── LightPlanUI              SwiftUI.
├── Spikes/                      прототипы, в приложение не тянутся
└── Tools/                       стенд паритета, генераторы знаков и словаря
```

**Почему локальные SPM-пакеты, а не папки.** Граница должна проверяться
компилятором. `LightPlanCore` физически не может импортировать SwiftUI —
математика не протечёт в UI даже случайно. Сегодня такой границы нет, и это
ровно тот вид ошибки, который просили не повторить.

**Почему не берём шаблон `~/Documents/LightPlan`.** Он собран мастером под
SwiftData с `Item`/`ContentView`; переиспользовать нечего. Проект заводится
внутри репозитория Light Plan, рядом с `docs/`, чтобы решения и код лежали в
одной истории.

**Язык:** Swift 6, строгая конкурентность. iOS 26 (шаблон уже на 26.5),
macOS — вторым таргетом, не первым.

### 4.2 Состояние: Observation, без фреймворка

```swift
@Observable final class MomentModel     // selDate, viewMin, place, skyMode
@Observable final class PlannerModel    // sessions, blocks, orgs, spots, trash
@Observable final class SettingsModel   // всё, что сегодня в saveAll
```

**Почему не TCA/Redux.** Экранов четыре, состояние маленькое и почти всё
производное. Фреймворк добавил бы слой, который нечем оправдать, и нарушил бы
`04_ANTI_GOALS` («не следуем моде»).

**Почему `@Observable`, а не `ObservableObject`.** Перерисовывается только то,
что читало изменившееся свойство. Для купола, который пересчитывается на каждое
движение пальца, это не оптимизация, а условие работы. Заодно исчезает
сегодняшняя ручная связка `renderToday(); renderMap(); renderCal();
renderDayPanel()` (8600), где всякая забытая перерисовка — ошибка.

### 4.3 Конкурентность

- Математика — **синхронная и чистая**. Акторов нет: солнце считается за
  микросекунды, `await` там был бы карго-культом.
- `actor Store` — единственная точка записи на диск.
- `actor WeatherCache`, `actor GeoCache` — сеть и её кэш.
- `async/await` для сети, файлов, геокодера, облака.
- Всё, что пересекает границу, — `Sendable` value types.

### 4.4 Данные: файл-документ, не SwiftData

**Решение: `Codable`-снимок в атомарно записываемый файл + папка вложений.**

1. Приложение уже так работает: всё в памяти, фильтрация в коде, запросов нет ни
   одного. SwiftData решала бы задачу, которой нет.
2. «Копия данных» — не служебная функция, а обещание продукта (инвариант 19).
   Когда снимок и есть формат хранения, копия честна по построению.
3. ROADMAP требует, чтобы копия стала **полным архивом** — с настройками,
   записями и самими документами. Документ и папка вложений упаковываются в один
   файл; графовая база — нет.
4. Схема будет меняться: миграций уже семь, писать их самому дешевле и виднее,
   чем через `VersionedSchema`.

Риск — рост данных. Тысяча съёмок это единицы мегабайт. Тяжёлое (референсы,
документы) в снимок не кладётся никогда, только пути. Точка возврата к
SQLite/GRDB зафиксирована и домен не ломает, потому что домен о хранилище не
знает.

### 4.5 Облако меняет роль

```swift
protocol CloudProvider {          // девять обязанностей, как сегодня
    func connect() async throws
    func putBackup(_ data: Data) async throws
    func getBackup() async throws -> Data
    func makeFolder(_ path: String) async throws
    func upload(_ file: URL, to path: String) async throws
    func viewURL(for path: String) async throws -> URL
    func delete(_ path: String) async throws
    func forget()
}
```

По ROADMAP облако из основного способа не потерять данные становится
дополнительным бэкапом и шлюзом выгрузки; главным хранилищем становится
файловая система. Поэтому облако переносится **поздно** (итерация 31), а не в
начале, как было бы по «важности» на глаз.

### 4.6 Погода: Open-Meteo остаётся основным

`sunsetScore` (7326) — сердце продукта — считается по трём ярусам облаков и
влажности **в час заката**. Open-Meteo отдаёт `cloud_cover_low/mid/high` и
`relative_humidity_2m` почасово, на 16 дней вперёд и 5 назад, без ключа и квоты,
одинаково на всех платформах.

WeatherKit заводится за тем же протоколом и **проверяется на этом конкретном
вопросе** (итерация 10): есть ли у него ярусы облаков в почасовом прогнозе и на
какую глубину. Менять работающий источник на предположение нельзя.

```swift
protocol WeatherSource { func hourly(at: Place, days: Int) async throws -> [HourWx] }
```

### 4.7 Локация

`CoreLocation` по запросу пользователя, `CLGeocoder` вместо двух веб-геокодеров.
Правила выбора и чистки имени (`cleanPlace`, `isCity`, `settlement`,
10149…10190) — это продукт, они переносятся поверх `CLPlacemark`.

### 4.8 Карта — открытый вопрос, решается прототипом

`MapKit` + свой слой прибора (`Canvas`), поворот через `MapCamera.heading`.

**Закрыто 20 сентября 2026** итерацией 4: делаются оба холста, выбор источника
живёт в настройках, плитки переезжают на свой Protomaps с приходом подписки.
Решение и числа — `docs/17_NATIVE_ARCHITECTURE.md` § 10 и `DECISIONS.md`.
Ниже — как вопрос стоял 26 августа; оставлено слепком.

Не решено было: **тёмный холст**. Сегодня это тайлы CARTO без подписей, перекрашенные
CSS-фильтрами, — так получен «холст, а не карта». MapKit произвольного стиля не
даёт. Три пути: (а) `.mapStyle` и тёмная тема, (б) `MKTileOverlay` с теми же
тайлами — тогда остаётся вопрос условий использования, (в) свой рендер векторных
данных. **Решение принимается на прототипе (итерация 4), а не здесь.**

### 4.9 Локализация и даты

`lang.js` → **String Catalog (`.xcstrings`)**, ключи 1:1. Плюрализация уезжает в
сам каталог: это ровно тот механизм, который в `lang.js` написан руками
(`RULES.ru` — три формы, `COUNT_SEP` для ja/zh). В шапке `lang.js` уже сказано:
«механизм подстановки в Swift не поедет, ценность в классификации».

Даты: `Intl` → `Date.FormatStyle`. `tools/datecheck.js` (2 376 проверок)
портируется в XCTest как есть. Генератор `Tools/lang2xcstrings.js` — чтобы до
конца миграции словарь правился в одном месте.

### 4.10 Знаки

`icons.js` — часть характера продукта. **SF Symbols её не заменяют и с ней не
смешиваются.** Генератор `Tools/icons2assets.js` разворачивает словарь в контуры
(`Path`) с теми же именами, а не в asset catalog: веб задаёт толщину линии
снаружи, а погода красит части по отдельности — каталог запекает и то, и другое
(из кода, каталог не мерили). Довод за генератор: 216 знаков руками не
переносят, и пока веб ещё правится, источник правды должен быть один.

### 4.11 Звук и отдача

Звук: сэмплы base64 → ресурсы, `AVAudioPlayer`.
Отдача: `.sensoryFeedback` / `UIImpactFeedbackGenerator`.

Это первый случай, где нативность **возвращает** уже спроектированное поведение:
снимаются три ограничения, записанные в коде (17240) — появляются разные тики,
отдача на событие и отдача на детентах ползунка и барабана.

Свитч под пальцем (`hapticize`, `hapticizeAll` — невидимый
`<input type="checkbox" switch>` поверх кнопки) — обход Safari, не замысел, и
в натив **не переносится** (Алексей, 21 сентября 2026: «по сути у нас вся
тактильность на костылях»). Замысел — где и как отзывается касание — берётся
из веба; способ — только системный.

### 4.12 Горизонт Event OS

Light Plan становится ролью Organizer в Event OS. Сегодня это не задача, но два
требования к архитектуре следуют уже сейчас, и стоят они дёшево:

- у записи есть **устойчивый идентификатор** (`id` уже есть, 16392) и он
  переживает правку — на нём держится ссылка опросника, а позже будет держаться
  связь с чужим календарём;
- домен не завязан на «одного фотографа»: `Session` не хранит владельца, и
  добавление владельца позже не должно переписывать хранилище.

Больше ничего под Event OS не проектируем: `BRIDGE_LIGHT_PLAN.md` — очередь
открытых вопросов, а не спецификация.

### 4.13 Выпуск

В первой редакции этого не было вовсе. Решается в итерации 5, делается в 34:
bundle id, подпись, профиль, App Store Connect, TestFlight для беты. Пока веб
живёт на Pages, нативная бета живёт в TestFlight — это и есть замена «беты
рядом с релизом».

---

## 5. Вычислительный слой и паритет

```
        SwiftUI View
             ↓ читает
   @Observable Model
             ↓ спрашивает
        Domain
             ↓
   Core: Solar · Moon · MilkyWay · Eclipse · Light · SunsetScore
             ↑
   Data: Weather · Location · Store · Cloud
```

`LightPlanCore` не импортирует ничего, кроме `Foundation`. Это проверяет
компилятор, а не договорённость.

### 5.1 Форма API

Сегодня `computeSun(date)` мутирует глобальные `SUN`, `decl`, `solarNoon`, и
чужой день считается приёмом «сохранить → посчитать → вернуть». Так работают
градиент ленты (16616), `buildWx` (7380), недельные полосы, `moonVsStars`. Это
семейство скрытых ошибок, а не стиль.

```swift
struct SolarDay {
    init(date: CivilDate, place: Place)   // смещение зоны берётся на эту дату
    init(date: CivilDate, latitude: Double, longitude: Double,
         utcOffsetHours: Double)          // ядро сверки: пояс явный и дробный
    let rise, set, goldenA, goldenB, blueA, blueB,
        civilA, civilB, nauticalA, nauticalB, astroA, astroB: Minutes?
    let solarNoon: Minutes
    let maxElevation: Degrees
    let polar: PolarState                 // .normal, .day, .night
    func elevation(at: Minutes) -> Degrees
    func azimuth(at: Minutes) -> Degrees
    func shadowRatio(at: Minutes) -> Double?
    func state(at: Minutes) -> LightState  // 15 кодов, ни одним больше
}
```

Значение-тип: соседний день — просто второй экземпляр. Приём save/restore
исчезает вместе с классом ошибок.

Поправка итерации 7. Вместо `tz: Int` — два входа (см. выше): пояс бывает
дробным (Катманду 5.75, Чатем 12.75), а день — это `CivilDate` из трёх чисел,
не `Date`: результат не зависит от зоны машины. `state(at:)` в типе появится
с итерацией 8.

### 5.2 Стенд паритета JS ↔ Swift

Приём уже изобретён в проекте: `tools/datecheck.js` **вырезает слой прямо из
`index.html` и исполняет его**. Повторяем для всей математики.

Сделано итерацией 6, числа измерены 20 сентября 2026 (`light_plan:native/`):

```
Tools/parity/extract.js    режет из beta/index.html шесть блоков по якорным
                           строкам, а не по номерам: номера уезжают
Tools/parity/generate.js   гоняет их по сетке входов, пишет фикстуры;
                           перед записью — самопроверка на 11 известных значениях
Fixtures/*.json            девять файлов, 396 837 точек, 10,2 МБ, в репозитории
                           (восемь и 393 349 точек, 8,0 МБ до окон неба 9а)
                           (325 994 и 6,9 МБ до ±90 итерации 7; 386 746 и 7,6 МБ
                           до луны с фазой и затмений итерации 9)
LightPlanCoreTests         читает фикстуры, сверяет Swift
make parity                пересобрать и доказать повторяемость прогона
```

Оценка «~50 000 точек» из первой редакции оказалась втрое занижена: одни
состояния света по § 5.3 — это 288 000 минут. Хранятся отрезками, поэтому
файл не раздувается.

Сетка входов — не «несколько дат наугад»:

- широты: −90, −78, −66.6, −45, −23.4, 0, 23.4, 45, 55.03 (Барнаул), 66.6, 78, 90
  (полюса добавлены итерацией 7);
- долготы: −179, −90, −1, 0, 1, 37, 90, 179;
- даты: солнцестояния и равноденствия ±1 день, первое число каждого месяца трёх
  лет, 29 февраля, дни перевода часов;
- время: каждые 10 минут суток плюс все границы состояний
  (−18, −12, −6, −4, −0.833, 0, 6, 6.05, 20, 40°).

Допуски: высота и азимут `1e-9°`; времена событий `1e-6` минуты; **коды
состояний, полярность и уровень прибора — строгое равенство**. Формула та же,
арифметика та же — расхождение означает ошибку переноса, а не «погрешность».

Допуск `6.05` на границе золотого часа (7534) намеренный. Тест обязан его
защищать, а не «чинить».

Поправка по замеру 20 сентября 2026. **Ровно на пороге строгое равенство кодов
недостижимо в принципе.** Обратный ход `tAtElev(h) → elevAt(t)` сходится до
`4e-14°`, и этих четырнадцати знаков хватает, чтобы точка легла по любую
сторону строгого «больше»: на 6.05° из 346 меток 145 вышли золотым часом,
201 — уже вечером. Поэтому пороги проверяются пробами в сотой градуса от них,
а сами метки лежат в фикстуре как показания. Отсюда же был риск: арифметика
IEEE у обоих языков одна, а тригонометрия библиотечная, и `Math.sin` в V8 могла
разойтись с `sin` в Darwin на последний бит. **Замерено в итерации 7 (21 сентября
2026): расходится, но до границ не доходит.** Наибольшая разница по высоте
`2.8e-14°`, по азимуту `1.1e-13°`, по временам событий `4.5e-13` минуты — при
допусках `1e-9°` и `1e-6`; ни одного расхождения «событие есть / нет» на
5 148 сутках × 12 событиях. Допуск на высоту не понадобился.

### 5.3 Что сверяется тем же стендом

| Слой | Фикстура | Строгость |
| --- | --- | --- |
| Луна: alt/az/dist/фаза | 6 широт × 36 дат (первое число месяца) × каждые 3 часа, плюс пробы дробных минут; фаза — каждые 6 часов трёх лет. Плотнее по времени луну гоняют восход и заход из 9а (шаг 10 минут) | `1e-7` |
| Млечный Путь | контрольные точки l=0 и l=180 (6930) | как в комментарии |
| `sunsetScore` | сетка low×mid×high×hum по 10 % | строго |
| `stateAt`, `nextLight` | все минуты × 20 дат × 12 широт | строго |
| Даты и склонения | порт `tools/datecheck.js`, 2 376 проверок | строго |
| Наложения, сроки сдачи, доход | сгенерированные записи | строго |
| Состав формы по жанрам | 12 жанров и незнакомый × 2 режима формы (съёмка, встреча) × 15 полей | строго |

### 5.4 Визуальный паритет — не фаза, а обязанность каждой экранной итерации

Поправка к первой редакции. Отдельная фаза «Visual Parity» в конце — место, где
такие проекты умирают: расхождения копятся месяцами и разбираются, когда уже
дорого. У проекта есть `tools/shot.js`, снимок за секунду.

Поэтому: **каждая экранная итерация закрывается сверкой снимка с вебом**, а
итерация 32 — только аудит остатков и приговор по каждому расхождению.

Исключение — стекло (21 сентября 2026). В вебе оно имитация (`backdrop-filter`,
26 мест в бете), в нативе — системное `.glassEffect`. Поверхности из стекла
сверяются по месту, размеру и порядку слоёв, а не по пикселям: расхождение
вида стекла с вебом — ожидаемое, не ошибка переноса.

---

## 6. Политика веба во время миграции

В первой редакции этого не было, а без этого переносим движущуюся цель. Веб
сегодня — действующая лаборатория дизайна, бета правится и пушится ежедневно.

**Три состояния, по очереди:**

1. **Лаборатория (сейчас).** Веб правится свободно. Нативного кода нет.
   Условие выхода названо Алексеем 26 августа 2026 и заменяет прежнюю формулу
   из ROADMAP: закончить **испанский перевод** и **пару фич**, после чего
   начинается миграция. Английский и китайский словари уже закрыты (`ff1c92d`,
   `c0fbbbf`). Три пункта ROADMAP «что стоит закончить на вебе» закрыты;
   «отвязать библиотеку знаков» значило встроить слепок `icons.js` в
   `index.html`, чтобы страница была одним файлом, — сделано в вебе (ROADMAP,
   «Ближайшее», пункт 1). Итерация 15 генерирует знаки натива из `icons.js`.
2. **Заморозка дизайна (с итерации 2).** На вебе чинятся только ошибки. Новые
   продуктовые решения принимаются, записываются в `docs/`, но реализуются
   сразу нативно. Довод: с этого момента всякая правка веба — работа, которую
   придётся сделать дважды.
3. **Веб — эталон (с итерации 18).** Веб не меняется вовсе: он становится
   измерительным прибором, с которым сверяются снимки. Правка эталона обесценит
   сверку.

Ветки: веб живёт в `main`, нативное — в `native`. Слияния между ними нет; общее
у них — `docs/`, `DECISIONS.md`, `ROADMAP.md`, и они правятся в обеих.

---

## 7. Правила остановки

Что делать, если прототип не дал ответа. В первой редакции этого не было, и план
подразумевал, что всё получится.

**Итерация 3 (таймбар) не удалась.** Признак: механика взвода и срыва не
воспроизводится с сохранением 60 кадров, либо жест конфликтует с системными.
Тогда: таймбар остаётся единственным незакрытым местом, а план идёт дальше —
экраны «Съёмки», форма и карточка от него не зависят вовсе (они живут на
`selDate`, не на `viewMin`). Экраны «Свет» и «Карта» получают временное
управление временем (шаг кнопками и шторка барабанов, они и так есть), и вопрос
возвращается отдельной вехой. **Чего делать нельзя:** упростить взвод и срыв до
кнопки «следующий день» молча — это инвариант 4.

**Итерация 4 (холст карты) не удалась.** Признак: ни один из трёх путей не даёт
монохромного холста, не спорящего с разметкой. Тогда решение — продуктовое, не
техническое, и вариантов три: карта на светлом стандартном стиле с усиленным
прибором; карта только как выбор места без прибора; перенос карты в самый конец.
Выбирает Алексей, не исполнитель.

**Общее правило.** Провалившийся прототип не отменяет миграцию и не
останавливает её. Он меняет порядок и объём — и это записывается в `ROADMAP.md`
в тот же день.

---

## 8. Итерации

Правило: каждая итерация оставляет проект собранным и запускающимся. Ни одна не
меняет UX самовольно — предложения уходят в «Potential Native Improvements».

Обозначение сложности: **Н** низкая, **С** средняя, **В** высокая,
**ОВ** очень высокая.

---

### Итерация 0 — Reverse engineering ✔ закрыта

**Цель.** Понять систему до единой строки Swift.
**Делаем.** Разбор кода, карта функций, данных и UI, вес работы числом, список
инвариантов, три корзины поведения.
**Не делаем.** Не пишем Swift, не заводим проект.
**Файлы.** `SWIFT_MIGRATION_PLAN.md`.
**Зависит от.** —
**Результат.** Настоящий документ.
**Готово, когда.** Алексей прочитал и выбрал первую итерацию.
**Риски.** Разбор мог что-то пропустить — правится документ, а не код.
**Тесты.** —
**Checkpoint.** `migration: разбор веб-версии и план переноса`
**Сложность.** В
```
Recommended: Opus
Reason: чтение 19 000 строк с выводом архитектуры и UX-инвариантов.
```

---

### Итерация 1 — Архитектура вчерне, политика веба и выпуска ✔ черновик

**19 сентября 2026.** Сделана как `docs/17_NATIVE_ARCHITECTURE.md` (номер 13
занят). Архитектура дальше живёт там; § 4 этого плана — слепок 26 августа.
Ветки `native` не будет: натив лежит в `light_plan:native/` со своим локальным
git (ответ Алексея, 19 сентября; § 2 документа).

**Цель.** Записать решения, которые не зависят от прототипов, и честно назвать
те два, которые зависят.
**Делаем.** `docs/13_NATIVE_ARCHITECTURE.md`: модули и границы, состояние,
конкурентность, хранилище, поставщики, локализация, знаки. Разделы «принято»,
«решается прототипом», «отвергнуто и почему». Политика веба (§ 6). Решения по
выпуску: bundle id, подпись, TestFlight. Ветка `native`.
**Не делаем.** Не закрываем вопросы карты и WeatherKit. Ни строки Swift.
**Файлы.** `docs/13_NATIVE_ARCHITECTURE.md`, запись в `DECISIONS.md`.
**Зависит от.** 0
**Результат.** Архитектура принята в той части, где для этого есть основания.
**Готово, когда.** У каждого решения довод, а не название технологии; открытые
вопросы названы открытыми.
**Риски.** Решить раньше проверки. Лечится тем, что открытые вопросы вынесены
явно.
**Тесты.** —
**Checkpoint.** `migration: архитектура вчерне и политика веба`
**Сложность.** В
```
Recommended: Opus
Reason: выбор между архитектурами, затрагивает все подсистемы сразу.
```

---

### Итерация 2 — Скелет проекта ✔ закрыта

**19 сентября 2026.** Сделана в `light_plan:native/` (ветки `native` нет, § 2
документа 17). Итог по числам:

- `LightPlan-iOS` (iPhone, только портрет, iOS 26) и `LightPlan-macOS` (macOS
  26, песочница) собираются; iOS собран для симулятора и для устройства с
  подписью команды `4A3PUKS9R9` (профиль уже был, к Apple не обращались);
- пять пакетов, по одному тесту в каждом; обе схемы гоняют все пять: 5 из 5
  на симуляторе iOS и 5 из 5 на macOS;
- настройки только в `Config/*.xcconfig`; иконка из `icon.svg`;
- проект написан вручную, без XcodeGen: ничего не устанавливалось. Это
  небольшой файл, потому что папки приложений синхронизированные (Xcode 16+).

**Замер, которого документ не ждал.** «`Core` физически не может импортировать
SwiftUI» оказалось неверным: `Core` с `import SwiftUI` собирается. SwiftPM
запрещает импорт чужих пакетов вне зависимостей, но не системных фреймворков.
Границу по ним держит `Tools/check_boundaries.sh`, фаза сборки обоих
приложений; проверена на десяти подсунутых нарушениях и на настоящей сборке.
Подробности — § 1 документа 17. Возможности бесплатной команды измерены
(§ 19): App Groups есть; iCloud, Push и WeatherKit нет.

**Цель.** Пустой, правильно разложенный проект, который собирается.
**Делаем.** Xcode-проект в репозитории; пять локальных SPM-пакетов; таргеты iOS
и macOS; Swift 6, strict concurrency; `.xcconfig`; иконка из `icon.svg`; схемы;
хук фильтрации вывода `xcodebuild` (есть в правилах проекта). Веб переходит в
состояние «заморозка дизайна».
**Не делаем.** Никакой логики. Один экран-заглушка.
**Файлы.** `LightPlan.xcodeproj`, `Packages/*/Package.swift`.
**Зависит от.** 1
**Результат.** Зелёная сборка на симуляторе и устройстве.
**Готово, когда.** Сборка приложения падает при попытке `import SwiftUI` в
`LightPlanCore` — граница проверена, а не заявлена. Сам пакет `Core` с таким
импортом соберётся: SwiftPM системные фреймворки не ограничивает.
**Риски.** Разъезд настроек между таргетами — лечится `.xcconfig`.
**Тесты.** Пустые тест-таргеты во всех пяти пакетах запускаются.
**Checkpoint.** `migration: скелет проекта и модули`
**Сложность.** Н
```
Recommended: Sonnet
Escalate to Opus if:
- деление на модули даёт циклы зависимостей;
- macOS-таргет требует другой формы проекта.
```

---

### Итерация 3 — Spike: таймбар ✔ закрыта

**Цель.** Доказать, что главный орган управления воспроизводим, **до** того как
под него написано хоть что-то.
**Делаем.** Отдельный таргет-песочница на выдуманных числах: свой ползунок на
`DragGesture` (нативный `Slider` не годится — нужен доступ к движению за
упором); ярус с рисками и дорожкой света; лента в двух видах (полоса и барабан);
пружина и осадка; натяжение яруса; срыв с вылетом и въездом с другой стороны;
отдача на детентах. Константы из веба: `WIND_NEED = 34`, `DWELL_RATE = 32`,
`STRAIN_MAX = 6`, `EDGE_ZONE = 28`, задержка 1.06 с.
**Не делаем.** Настоящих данных, солнца, встраивания в приложение.
**Файлы.** `Spikes/TimebarSpike/*`.
**Зависит от.** 2
**Результат.** Ответ «да, так» или «нет, вот так», записанный в `DECISIONS.md`.
**Готово, когда.** На живом телефоне: срыв срабатывает пальцем, упёртым в бортик
чехла; за одно удержание ровно один переход; метка не дрожит; 60 кадров при
ведении; барабан не липнет.
**Риски.** Главный риск всей миграции. Веб стоил нескольких заходов — разделы
`DECISIONS.md` «Передача разряда» и «Лента без нативного скролла» читаются **до**
прототипа, а не после.
**Тесты.** Замер кадров инструментами; ручная проверка на устройстве.
**Checkpoint.** `migration: прототип таймбара`
**Сложность.** ОВ
```
Recommended: Opus
Reason: неоднозначная задача с историей отвергнутых решений; цена ошибки —
переделка главного органа управления после того, как под него написаны экраны.
```

---

### Итерация 4 — Spike: холст карты ✔ закрыта

**Цель.** Ответить на единственный вопрос, который может изменить продукт, а не
только код.
**Делаем.** Три пути из § 4.8 рядом, на одном месте, в обеих темах, на живом
телефоне: `.mapStyle` MapKit; `MKTileOverlay` с тайлами CARTO (вместе с разбором
условий использования); свой рендер. Поверх каждого — упрощённый прибор
(лимб, две оси, путь солнца), чтобы видеть, спорит холст с разметкой или нет.
**Не делаем.** Не рисуем полный прибор. Не пытаемся повторить CSS-фильтры.
**Файлы.** `Spikes/MapCanvasSpike/*`.
**Зависит от.** 2
**Результат.** Выбранный путь либо срабатывает правило остановки (§ 7).
**Итог 20 сентября 2026.** Сравнивались два пути (§ 22 архитектуры), а не три.
Правило остановки не сработало: холст, не спорящий с разметкой, получен —
MapLibre читает `beta/mapstyle.js` без переделки. Решение Алексея: делаются
оба, переключатель источника в настройках, Protomaps с приходом подписки.
Песочница — `native/Spikes/MapCanvasSpike/`, снимки — `Shots/`.
**Готово, когда.** Решение записано в `DECISIONS.md` с картинками трёх путей.
**Риски.** Ответа может не быть — на этот случай § 7.
**Тесты.** Снимки трёх путей × две темы × город и природа.
**Checkpoint.** `migration: прототип холста карты`
**Сложность.** В
```
Recommended: Opus
Reason: выбор между тремя несравнимыми путями с внешними ограничениями;
результат меняет продуктовый план, а не только реализацию.
```

---

### Итерация 5 — Архитектура закрыта ✔ закрыта

**Цель.** Дописать документ архитектуры по итогам двух прототипов.
**Делаем.** Закрываем вопросы карты и таймбара; фиксируем, что изменилось в
плане; при срабатывании правила остановки — переписываем порядок итераций.
**Не делаем.** Не начинаем экранов.
**Файлы.** `docs/13_NATIVE_ARCHITECTURE.md`, `ROADMAP.md`, `DECISIONS.md`.
**Зависит от.** 3, 4
**Результат.** Архитектура без открытых мест.
**Готово, когда.** Ни один пункт не помечен «решается прототипом».
**Итог 20 сентября 2026.** Пометка «прототип» снята из документа целиком, § 10
переписан решением, § 9 закрыт по сервисам карты, вопрос 6 Алексею закрыт.
Открытыми остались два вопроса к Алексею (полевой прогон, копия кода вне Mac)
и один замер «сверить» (условия Apple на маршрут поверх чужого холста).
**Риски.** —
**Тесты.** —
**Checkpoint.** `migration: архитектура закрыта`
**Сложность.** С
```
Recommended: Opus
Reason: сведение результатов прототипов в решения.
```

---

### Итерация 6 — Стенд паритета ✔ закрыта

**Цель.** Механизм, доказывающий, что Swift считает ровно то же, что JS.
**Делаем.** `Tools/parity/extract.js` (приём из `tools/datecheck.js`),
`generate.js`, сетка входов § 5.2, фикстуры в `Fixtures/`, загрузчик в тестах,
`make parity`.
**Не делаем.** Сам движок не пишем.
**Файлы.** `Tools/parity/*`, `Fixtures/*.json`.
**Зависит от.** 5
**Результат.** Фикстуры в репозитории, тест-заглушка их читает.
**Готово, когда.** Повторный прогон даёт побайтово тот же файл.
**Итог 20 сентября 2026.** Стенд собран, `eb885e3` и `5bb19ea`. Восемь блоков
режутся из живой беты по якорным строкам; семь фикстур на 325 994 точки,
6,9 МБ; 11 известных значений в самопроверке генератора; 12 тестов в
`LightPlanCoreTests` читают фикстуры и проходят и через `swift test`, и через
схему проекта. Повторяемость доказана: `make parity` гоняет генератор дважды и
сличает файлы. Сверх плана взято слияние снимков парами — этого требует § 7
архитектуры. Три замера по дороге изменили написанное: состояний света
пятнадцать, а не тринадцать (§ 5.1); ровно на пороге сторону выбирает
последний бит (§ 5.2); слияние симметрично по содержанию, но не по порядку
записей.
**Риски.** Вырезание слоя ломается при правке беты — границы ищутся по якорным
строкам, промах даёт внятное падение, а не тихую подмену.
**Тесты.** Самопроверка генератора на трёх известных значениях.
**Checkpoint.** `migration: стенд сверки JS и Swift`
**Сложность.** С
```
Recommended: Sonnet
Escalate to Opus if:
- слой не вырезается из-за переплетения с UI и нужен другой способ снять эталон.
```

---

### Итерация 7 — Solar Engine ✔ закрыта

**Цель.** Солнце в Swift, доказанно совпадающее с вебом.
**Делаем.** `SolarDay` по § 5.1: склонение, уравнение времени, солнечный
полдень, высота, азимут, времена по высотам, длина тени, полярный день и ночь,
дуга купола.
**Не делаем.** Ни луны, ни погоды, ни UI, ни состояний света.
**Файлы.** `LightPlanCore/Solar/*`.
**Зависит от.** 6
**Результат.** Чистый тип, ноль глобалов.
**Готово, когда.** Фикстуры солнца проходят с допусками § 5.2; полярные широты
проверены отдельно; 10 000 экземпляров `SolarDay` строятся быстрее, чем кадр —
купол пересчитывается на каждое движение пальца.
**Итог 21 сентября 2026.** `SolarDay` в `LightPlanCore/Solar/`: значение-тип,
ноль глобалов, порядок операций тот же, что в JS. Два входа: ядро сверки с явным
дробным смещением и тонкий `SolarDay(date:place:)`, который берёт смещение на
дату из зоны места; день — `CivilDate` (год, месяц, число), поэтому результат не
зависит от зоны машины. Сверено: 5 148 суток × 20 полей и 19 102 точки (120
рядов) × высота, азимут, тень. Наибольшее расхождение: высота `2.8e-14°`,
азимут `1.1e-13°`, время события `4.5e-13` минуты, склонение `5.6e-17` рад —
при допусках `1e-9°` и `1e-6`; «нет события» и полярность совпали без
исключений, в debug и в release одинаково. Полюса ±90° добавлены в сетку
широт (фикстуры: 4 316 → 5 148 суток, 104 → 120 рядов, 386 746 точек, 7,6 МБ);
краевые 66.6°, 29 февраля 2028 и четыре дня перевода часов уже были в сетке.
Скорость, release, Apple M1 Pro: 10 000 суток — **1,53 мс** ядром и
**6,2–6,4 мс** с зоной места (кадр 120 Гц — 8,33 мс; лишние ~4,8 мс — это
вход с зоной, то есть `Calendar` и смещение на дату; профиль не снимали, не
оптимизировалось). Тест способен падать: с целым смещением вместо дробного он
падает на Катманду на 45 минут. `swift test` и
`xcodebuild test` через `LightPlan-iOS` зелёные, границы слоёв в порядке.
Записаны: момент отсчёта смещения (DECISIONS), пункт 15 корзины 3.
**Риски.** Потеря точности на `Float`; часовой пояс как источник расхождений.
Лечится: везде `Double`, пояс — явный параметр, не системный.
**Тесты.** Фикстуры + краевые: 66.6°, 90°, перевод часов, 29 февраля.
**Checkpoint.** `migration: солнечный движок`
**Сложность.** С
```
Recommended: Sonnet
Escalate to Opus if:
- расхождение с JS не объясняется за один заход;
- выясняется, что веб-формула сама неточна — тогда это отдельное решение,
  а не тихая правка: паритет важнее правоты.
```

---

### Итерация 8 — Состояние света и палитра ✔ закрыта

**Цель.** 15 состояний света и их цвета.
**Делаем.** `LightState` (код, уровень прибора, тон, цвет, зарево),
интерполяция палитры, `shadowWord`, `nextLight`.
**Не делаем.** Цвета интерфейса не трогаем — только модель света.
**Файлы.** `LightPlanCore/Light/*`.
**Зависит от.** 7
**Результат.** `state(at:)` возвращает то же, что `stateAt`.
**Готово, когда.** Строгое равенство кодов на всей сетке; допуск `6.05` защищён
именованным тестом.
**Итог 21 сентября 2026.** Коммит `f35234a` в `light_plan:native/`, запушен.
`LightState` (код из 15, уровень прибора, тон, зарево, цвет неба, звёзды),
`LightPalette` (восемь цветов, `lerp`, лестница `skyColor`), `ShadowWord`,
`NextLight`; `SolarDay.state(at:)`, `shadowWord(at:)`, `nextLight(at:)`. Сверено с
фикстурой: 345 600 минут × код, уровень, тон, зарево, звёзды, слово о тени,
подпись и остаток до события, плюс цвет неба каждые 10 минут — **0
расхождений**; остаток до события расходится не больше чем на `2.3e-13` минуты
при допуске `1e-6`. Пробы в сотой градуса от порогов совпали строго. Тесты
способны падать: порог 6.05 → 6 роняет десять проверок, «отсутствующее событие»
как «после всех» роняет сверку минут. `swift test` (34 теста) и `xcodebuild test`
через `LightPlan-iOS` зелёные, границы слоёв в порядке.
Три уточнения по дороге. (1) Уровень прибора у астрономических сумерек и ночи —
`nil`, как в вебе (`level` там не задан): прибор уровень не показывает.
(2) `nextLight` веба читает отсутствующее событие (`null`) нулём; замер: 11 суток
из 240 (Антарктида и 66.6° в июне), и там это «пропустить ветку» — нет золотого
часа, сразу к закату; нет синего, сразу к рассвету. Ошибки нет, в корзину 3 не
идёт; перенесено буквально и закреплено двумя сутками в тесте. (3) Метки ровно
на пороге расходятся на 6 порогах из 8 (по 2–6 меток), как и предсказывал § 5.2:
сторону там выбирает последний бит; проверяется на пробах, а не на метках.
Слова состояния (`label`, `sense`, `light`, `rec`) в модель не вошли — они из
словаря по `code`, итерация 14.
**Риски.** Соблазн причесать пороги. Запрещено: это физика продукта.
**Тесты.** Фикстуры состояний; границы −18/−12/−6/−4/−0.833/0/6/20/40.
**Checkpoint.** `migration: состояние света и палитра`
**Сложность.** Н
```
Recommended: Sonnet
Escalate to Opus if: —
```

---

### Итерация 9 — Луна, Млечный Путь, затмения ✔ закрыта ∥

**Цель.** Остальное небо.
**Делаем.** `MoonSample` (alt/az/dist/фаза), `galToEq`, `eqToAltAz`, полоса МП с
переменной полушириной, `MW_BAND` константой, таблица затмений в ресурс.
**Не делаем.** Не считаем затмения формулой — расчёт отвергнут трижды, причина
записана (7130).
**Файлы.** `LightPlanCore/Moon/*`, `MilkyWay/*`, `Eclipse/*`.
**Зависит от.** 7
**Результат.** Небо считается целиком.
**Готово, когда.** Контрольные точки сходятся (l=0 → 266.405 / −28.936;
l=180 → 86.405 / +28.936); фикстуры луны проходят; затмения совпадают по датам и
фазам.
**Итог 21 сентября 2026.** Коммит `20a261a` в `light_plan:native/`, запушен.
`MoonSample` (высота, азимут, расстояние; фаза — по требованию: `phase.fraction`,
`phase.cycle`, `phase.name` кодом словаря), `MilkyWay` (`band` — 121 точка
константой, `core`, `halfWidth`, `equatorial(galactic…)`, `corePosition`),
`EclipseTable` (`on`, `next`; таблица — ресурс пакета `Eclipse/Resources/
eclipses.json`, ложится в `.app`, проверено сборкой). Общее у луны и Млечного
Пути вынесено в `Sky/`: момент, дни от J2000, перевод «точка неба → над
головой» — в вебе это две копии одной формулы, здесь одна. Сверено: луна
1 848 точек — наибольшее расхождение **`1.1e-13°`** (азимут) при допуске `1e-7`;
фаза 4 384 отсчёта за три года, доля диска `4.4e-16`, имена совпали все, за три
года встретились все восемь; полоса 121 точка и ядро 48 точек — `5e-15`;
затмения — таблица и `on` / `next` на каждые сутки 2025-06-01…2031-02-28
совпали строго. Контрольные точки Млечного Пути (l=0 → 266.405 / −28.936,
l=180 → 86.405 / +28.936) стоят отдельным тестом. Скорость, release, M1 Pro:
10 000 отсчётов луны — **1,4 мс** положением и 3,5 мс вместе с фазой (кадр 120 Гц —
8,33 мс). Тесты способны падать: без обрезки миллисекунд луна уходит на
`1.4e-6°`, граница фазы 0.28 → 0.281 роняет сверку имён, испорченная строка
таблицы роняет два теста. `swift test` (48 тестов вместе с итерацией 8),
`xcodebuild test` через оба приложения и сборка `LightPlan-iOS` зелёные,
границы слоёв в порядке. Работали параллельно с итерацией 8, в своём worktree,
файлов не пересеклось.
Три уточнения по дороге. (1) **Веб обрезает дробные миллисекунды**: момент
собирается через `new Date(мс)`, а он отбрасывает дробную часть. Ползунок даёт
дробные минуты, луна уходит на `4e-6°` за миллисекунду — больше допуска, — и
Swift обрезает так же (`Sky.days`); в фикстуру добавлены «страшные» минуты
(`probes`). (2) Фаза не входила в фикстуры луны вовсе, и `moonPhase` стоял в
бете вне вырезаемого блока — стенд получил три блока (`sameDay`, `eclipse`,
`moonPhase`), отпечаток вырезки сменился, все фикстуры пересобраны, добавлен
`eclipse.json`: теперь 8 файлов, 393 349 точек, 8,0 МБ. (3) Сетка луны в § 5.3
названа «каждый час», а построена каждые 3 часа и только на первое число месяца
(1 728 точек); для сверки формулы этого хватило (расхождение на тринадцатом
знаке), но по § 5.3 это неполно — пометка, не переделка.
**Найдено, не сделано — назначено итерации 9а** (21 сентября 2026, решение
исполнителя по поручению Алексея): восход и заход луны (`moonArc`,
`moonCross`), окно Млечного Пути (`mwWindow`) и `moonVsStars`. В § B.1 их не
было, этой итерацией не назначены.
**Риски.** Точность луны заявлена низкой — не улучшать при переносе, это
изменило бы поведение продукта.
**Тесты.** Фикстуры и контрольные точки из комментариев.
**Checkpoint.** `migration: луна, млечный путь и затмения`
**Сложность.** С
```
Recommended: Sonnet
Escalate to Opus if:
- контрольные точки МП не сходятся после одной попытки.
```

---

### Итерация 9а — Окна неба: луна над горизонтом, окно Млечного Пути ✔ закрыта ∥

**Цель.** Три расчёта поверх солнца, луны и Млечного Пути, которые план не
назначил ни одной итерации (найдены итерацией 9).
**Делаем.** Восход и заход луны (`moonArc`: шаг 10 минут по окну −720…+2900,
уточнение `moonCross` девятью делениями пополам, округление до минуты; дуга,
в которой лежит время, иначе ближайшая следующая, иначе «не всходит»). Окно
астросъёмки (`mwWindow`: шаг 5 минут от `MINT` до `MAXT`; лучшая высота ядра,
оболочка `from`–`to`, настоящие отрезки `spans`, `dark`, `moonBlocks`). Помеха
луны для звёзд (`moonVsStars`: доля тёмной части с луной над горизонтом,
наибольшая освещённость, уровень 0/1/2). Всё — значения в `LightPlanCore`, без
глобалов: `moonVsStars` в вебе считает чужой день приёмом «сохранить →
посчитать → вернуть» (`computeSun(keep)`), здесь это второй `SolarDay`.
**Не делаем.** Погоду над окном (`mwSkyAt`) — итерация 10. Показ на экране —
купол (18) и «Свет» (19). Кэш `moonCache` по строке даты — только если замер
скорости попросит; в вебе он оптимизация, не смысл.
**Файлы.** `LightPlanCore/Moon/*`, `LightPlanCore/MilkyWay/*`, стенд:
`Tools/parity/*`, `Fixtures/*`.
**Зависит от.** 7, 9
**Результат.** Небо отвечает не только «где», но и «когда»: луна над
горизонтом, окно для Пути, мешает ли луна.
**Готово, когда.** Стенд вырезает новые блоки из беты по якорям, фикстуры
пересобраны (`make parity` — «побайтово то же»); восход и заход луны, окно и
его отрезки, помеха луны совпадают строго; полярные широты (луна не всходит,
тёмной части нет) проверены отдельно.
**Итог 21 сентября 2026.** Коммит `52de5af` в `light_plan:native/`, запушен.
`MoonDay` (`arcs`, `arc(at:)` — `nil` там, где веб отвечает `none`), `MilkyWayWindow`
(`best`, `from`, `to`, `spans`, `dark`, `moonBlocks`), `MoonVsStars` (`dark`, `lit`,
`share`, `percent`, `level` 0/1/2), плюс `MilkyWay.workLow/workHigh`. Значения без
глобалов; `moonCache` веба не переносился — дуги принадлежат экземпляру. Стенд
режет из беты ещё четыре блока (`skyWindows`, `milkyWayAt`, `moonArc`, `mwWork`) и
пишет `Fixtures/sky_windows.json`: 3 488 суток (сетка 12 широт × 3 долготы плюс
3 места с некруглым поясом × даты § 5.2, и каждые сутки 2026 в четырёх местах),
5 159 дуг, 59 004 ответа `moonArc`, из них 8 327 «не всходит». Теперь 9 файлов,
396 837 точек, 10,2 МБ. `make parity` — «побайтово то же»; в восьми старых
файлах изменился только отпечаток вырезки. Сверено строго: минуты дуг и
ответов, оболочка и отрезки окна, `dark`, `moonBlocks`, доля и уровень помехи;
высота ядра — `2.8e-14°` при допуске `1e-9`, освещённость — `5.6e-16`.
`swift test` (53 теста), `xcodebuild test` через оба приложения, границы слоёв —
зелёные. Скорость, release: дуги луны за сутки **0,039 мс**, окно **0,010 мс**,
помеха **0,008 мс**.
Тесты способны падать: из 13 порч кода ловятся 9 (8 делений вместо 9, заход
строго, порог луны 0.41, шаг окна 6, порог доли 0.11, шаг помехи 11, шаг луны 20,
нет запасной дуги; округление `.rounded()` — отдельным тестом). Не ловятся
четыре, и это не дыра сетки, а эквивалентные порчи: шаг луны 5 (обе сетки лежат на
одной двоичной решётке `10/512`, ответ бит в бит тот же), `0.25` нестрого и `≤ −18`
против `< −18` (нужно ровное равенство double), `previous > 0` вместо `≥ 0`.
Три уточнения по дороге. (1) **Риск «ровно на половине минуты» не реализуется:**
середина после девяти делений — целое плюс нечётное кратное `10/1024`, а `0.5`
чётное; в 889 округлениях ровно `.5` нет. (2) **На полюсе «лучшая минута» ядра —
шум:** высота плоская до `1e-14°`, максимум выбирает последний бит библиотечных
`sin/cos`; 111 суток на −90°. Там сверяется высота, минута нет. (3) **На полюсе веб
не находит дуг луны никогда** (365 из 365), потому что окно двое с половиной суток
не видит второго пересечения; на 78° ответ «не всходит» 245 суток из 365. Это
поведение веба, не ошибка переноса, — записано, не тронуто.
**Найдено, не сделано.** `hasAstroNight` и `nextAstroNight` (темнота в
произвольную дату и поиск возвращения темноты, `beta/index.html`, рядом с
`MW_HAZE`) не назначены ни одной итерации — чистая математика той же природы;
предлагаю добавить в итерацию 10 или 18.
**Риски.** Округление `moonCross` до минуты: ровно на половине сторону выбирает
последний бит, как у порогов света (§ 5.2) — проверять пробами, не подгонять.
В `moonVsStars` строка `x % 1440 === x ? x : x` ничего не делает — перенести
как есть и не «чинить». Шаг 5 и 10 минут — поведение продукта, не
оптимизировать.
**Тесты.** Фикстуры по сетке луны и широт § 5.2; ±90°; дни, когда луна
всходит и не заходит в пределах окна.
**Checkpoint.** `migration: окна неба`
**Сложность.** С
```
Recommended: Sonnet
Escalate to Opus if:
- расхождение с JS не объясняется за один заход.
```

Решение, почему отдельной итерацией, а не в 10 или 18: все три расчёта —
чистая математика неба без сети и без экрана, стенд сверки уже умеет луну и
Млечный Путь, а погода (10) сама читает `spans` окна через `mwSkyAt` и потому
должна идти после. DECISIONS, «Окна неба — итерация 9а».

---

### Итерация 10 — Погода и закатный балл ✔ закрыта ∥

**Цель.** Прогноз, балл заката и честный мок.
**Делаем.** `WeatherSource`, `OpenMeteoSource`, `buildWx` → `WeatherDay`,
`sunsetScore`, `deriveQ`, `scoreCat`, погода над окном Млечного Пути
(`mwSkyAt`, усредняет по `spans` из 9а), темнота в произвольную дату и её
возвращение (`hasAstroNight`, `nextAstroNight`: склонение на полдень места,
поиск вперёд до 190 суток — назначены 21 сентября, найдены итерацией 9а),
детерминированный мок с `isReal: false`,
кэш по (место, день), дебаунс при перетаскивании карты, тихий откат офлайн.
Проверяем WeatherKit на ярусы облаков и **записываем результат проверки**.
**Не делаем.** Ничего не показываем на экране.
**Файлы.** `LightPlanData/Weather/*`, `LightPlanCore/Sunset/*`.
**Зависит от.** 7, 8, 9а
**Результат.** Прогноз приходит, балл считается, офлайн не падает.
**Готово, когда.** `sunsetScore` совпадает строго; `hasAstroNight` и
`nextAstroNight` совпадают строго на сетке широт § 5.2, включая полярные, где
темнота за 190 суток не возвращается; без сети — мок и ни одной ошибки на
экране; повторный запрос того же места в сеть не ходит.
**Риски.** Сегодня ответ Open-Meteo меняет глобальный `TZ` (7433) — в Swift пояс
становится свойством `Place`, а не побочным эффектом загрузки погоды.
**Тесты.** Фикстуры балла; офлайн; смена места на лету (старый ответ не должен
перебить новый).
**Checkpoint.** `migration: погода и закатный балл`
**Сложность.** С
```
Recommended: Sonnet
Escalate to Opus if:
- WeatherKit оказывается лучше по ярусам и встаёт вопрос смены источника.
```
**Итог 22 сентября 2026.** Коммит `5a91abd` в `light_plan:native/`, ветка
`wt/iter10` — не запушен (классификатор авто-режима отказал в push, как и у
итерации 13; нужен ручной пуш Алексея: `git push origin wt/iter10:main`).
`LightPlanCore/Sunset/`: `SunsetScore` (`score`, `category`, `deriveQuality`,
`bell`), `AstroNight` (`has`, `next`), `WeatherDay`/`HourRecord`/`HourlyWeather`
(`buildDays` — порт `buildWx`), `MilkyWaySky` (порт `mwSkyAt`), `MockWeather`
(порт `mulberry32`, `qualityOf`, `dayWeather`, `real: false`), `AirSample` +
`Weather.nearHour`/`airWord`. `CivilDate` получил `adding(days:)` (общая
арифметика дней для `astroNight` и `mwSkyAt` — обратный алгоритм Хиннанта);
`ZoneID` получил `init(fixedOffsetHours:)` — явное смещение без базы IANA, для
входов, где пояс уже известен числом (стенд паритета и запасной путь без
сети). `LightPlanData/Weather/`: `WeatherSource` (протокол) + `OpenMeteoSource`
— те же переменные и то же окно прогноза, что у веба, воздух вторым запросом;
`WeatherFetcher` (`actor`) кэширует по месту (три знака после запятой); `WeatherStore`
(`@MainActor @Observable`) — дебаунс 500 мс (веб `wxTimer`), офлайн и отказ
молча оставляют выдумку, ответ про место, откуда карту уже увели, отбрасывается
сверкой места, как у `CurrentPlace` (не только отменой задачи).

Стенд: `extract.js` режет восемь новых блоков (`astroNight`, `mockWx`,
`wxState`, `airWord`, `buildWx`, `airState`, `airAt`, `mwSky`); четыре новые
фикстуры — `astro_night.json` (672 точки), `mock_weather.json` (368),
`weather_day.json` (9 суток, семь сценариев), `mwsky.json` (7 сценариев) — тот
же отпечаток вырезки, что у остальных девяти. `weather_day.json` и `mwsky.json`
несут синтетический почасовой вход целиком (без сети, без `Math.random`) —
Swift кормится тем же, чем кормили `buildWx`/`mwSkyAt` в JS, а не второй копией
генератора. Сверено строго: `sunsetScore` на всей сетке `sunset_score.json`
(готовой ещё до этой итерации) — 0 расхождений; `hasAstroNight`/`nextAstroNight`
на сетке широт § 5.2, включая полюса (там она не возвращается за 190 суток);
`buildDays` и `MilkyWaySky.over` — на всех сценариях, включая полярный день
(часа заката нет), дробный пояс (Катманду), только обязательные поля ответа
(запасные значения), туман и дождь, отрезок окна через солнечную полночь в обе
стороны. `swift test`: `LightPlanCore` 60 тестов/11 наборов, `LightPlanData`
33 теста/9 наборов — зелёные; `xcodebuild test` через `LightPlan-iOS` —
TEST SUCCEEDED; `Tools/check_boundaries.sh` — в порядке (5 пакетов, 48 файлов).

Риск плана снят предсказанным способом: Open-Meteo больше не двигает часовой
пояс как побочный эффект загрузки погоды — пояс остаётся свойством `Place`,
источник его геокодер (итерация 13), не прогноз.

**Не сделано.** Показ на экране (по плану — не в этой итерации). Проверка
WeatherKit на ярусы облаков — заблокирована тем же измерением итерации 2:
бесплатная команда `4A3PUKS9R9` не даёт WeatherKit («Personal development
teams … do not support the … capability»); повторно проверять не на чем,
пока команда не сменится.

**Найдено, не исправлено** (вне объёма итерации; запись в SESSIONS_CHAT.md
22 сентября 2026): `make parity` в `Makefile` сравнивает всю папку `Fixtures/`
с временной, а та не содержит `lang.json`/`format.json`/`location.json` —
файлы других генераторов (`make lang`, `location.js`). С тех пор, как эти
файлы появились (итерация 14), `make parity` падает на последнем шаге
(«Only in Fixtures: …»), хотя сама сверка фикстур повторяема — проверено
отдельно, `cmp -s` побайтово совпал на всех тринадцати файлах, включая
воспроизведённое поведение до правки этой итерации (`git stash`, тот же
сбой на неизменённом `generate.js`).
**Закрыто 22 сентября 2026** отдельным коммитом `8a216f3` — `Makefile`
сверяет явный список файлов генератора, а не всю папку (DECISIONS).

---

### Итерация 11 — Домен: записи, жанры, правила ✔ закрыта ∥

**Цель.** Съёмка и всё вокруг неё — типами.
**Делаем.** `Session`, `RoutePoint`, `Org`, `Block`, `Spot`, `TrashedItem`;
перечисления `Genre`, `GenreGroup`, `PayKind`, `DocKind`, `Wish`, `BlockKind`,
`RefTag`; таблицы `GENRE`, `GROUP`, `GENRE_TAGS`, `GENRE_DEADLINE`,
`GENRE_PERSONS`; правила: состав формы по жанру, наложения, фаза события,
`lightCase`, сроки сдачи, доход и расход, цепочка сделки, порядок блоков.
**Не делаем.** Ни хранилища, ни формы, ни карточки.
**Файлы.** `LightPlanDomain/*`.
**Зависит от.** 8
**Результат.** Правила продукта живут отдельно от экрана.
**Готово, когда.** Таблицы совпадают позиция в позицию; деньги — `Decimal`; ни
одного строкового ключа в сравнениях.
**Итог 22 сентября 2026.** Коммит `9d4f4df` в `light_plan:native/`, запушен
вместе с `5a91abd` итерации 10: оба добавили в `CivilDate` один и тот же
`adding(days:)`, поэтому 11 легла поверх 10 и дубль снят. Ядро:
`Time/WallTime.swift` — `WallTime` (день плюс минуты от полуночи первого дня,
бывают больше 1 440), `Moment` (целые миллисекунды), счёт дней у `CivilDate`.
Типы времени легли в Core рядом с `CivilDate` и `ZoneID`, а не в Domain, как
говорил `docs/17` § 22: их ждут и Timeline, и Data, а Timeline домена не видит.
Domain: `Genre` (12; порядок списка «Моих жанров» и порядок таблицы `GENRE` —
разные, оба перенесены), `GenreGroup`, `GenreSpec`, `GroupSpec`, `GenreProfile`
(умолчания веба для незнакомого жанра), `SubGenre` (41), `RefTag` (39 и
`TAG_CODE`), `PersonRole`, `PayKind`, `Currency`, `Wish`, `BlockKind`, `DocKind`
(с угадыванием по имени файла), `RecordKind`; записи `Session` (все поля
`saveAll`, имена свои, ключ веба у каждого поля в комментарии), `RoutePoint`,
`Attachment`, `Org`, `Block`, `Spot`, `Studio` с залами, `TrashedItem`, `Repeat`;
правила — `FormShape`, `Delivery` (срок и ступень сдачи), `Money`, `DealChain` с
`Practice`, `CardOrder`, `EventPhase`, сутки съёмки, `Nest` (матрёшка: промежуток
подсущностей, «уже содержимого нельзя», ручки у предела, перенос только
пустой), `Stops` (где спрашивать небо), `Overlaps` (наложения моментами, пояс
через протокол `ZoneResolving`), `LightCase`. Data: `Spot` встал на `Located`
(итерация 13 ждала), `ZoneCache` отвечает правилам поясом. Сверено своим
эталоном `Tools/parity/domain.js` (`make domain`, 37 якорных блоков, свой
отпечаток, общий стенд не тронут; `Fixtures/domain.json`, 1,8 МБ): таблицы
позиция в позицию, форма 26 случаев, сроки 816 + 14 + 154, доход 192, доля месяца
17, суммы по валютам 36, сделка 420 + 52, порядок блоков 195 + 39, фаза 306, сутки
63, матрёшка 14, места 9, свет 1 386 (825 со сказанным словом), вид документа
45, наложения — 2 220 сценариев, 4 349 проверок, все восемь родов — **0
расхождений**. Тесты способны падать: 16 порч правил дали падения в своих
тестах; первая сетка наложений не задевала границ порогов дороги — ни одна
порча там не падала, — добавлен набор границ. 41 тест Domain и 2 Data;
`xcodebuild test` через `LightPlan-macOS` (60 + 41 + 1 + 35 + 25) и сборка
`LightPlan-iOS` зелёные, границы слоёв в порядке.
Пять уточнений по дороге. (1) **`Codable` с ключами веба не сделан** — это
читатель формата, итерация 12; поля один к одному с `saveAll`, тесты читают
фикстуры своим построителем с умолчаниями веба. (2) **Механик оплаты четыре, а
не три:** помесячная доля повтора (`repShare`, `repSumAt`, `repPeriodIndex`)
перенесена — без неё доход помесячной карточки ноль; даты группы (`repDates`) —
форма, итерации 23–24. (3) **Незнакомый код читается пустым** и получает
умолчания веба: жанр — группа «люди», пресет «ставка или сдельно», срок 7 дней;
вид занятости — «занят». Незнакомый способ оплаты веб считает сдельным — решает
читатель (12). (4) **Координаты живут парой:** одинокая широта без долготы —
«точки нет» (веб местами читает долготу нулём; форма таких записей не делает).
(5) **Деньги — `Decimal`, веб — двоичное число:** «1234 ₽ × 50 минут» у веба
1028,3333333333335, здесь 1028,(3); сверка до двенадцатого знака, целые,
которые видит человек, — строго.
**Повторено за вебом, не чинить (паритет дороже):** `doneAt` — минута суток по
часам телефона, а сравнивается с минутами шкалы места; место без координат
при поиске закладки читается нулём (`Math.abs(null − lat)`); смещение пояса
одно на всю запись — на её день, даже когда съёмка уходит за полночь в сутки с
другим смещением.
**Найдено, не сделано.** (1) `make parity` падает на файлах, которые пишут другие
скрипты (запись итерации 10 в DECISIONS), — `domain.json` добавил в этот список
ещё один. **Закрыто 22 сентября 2026** вместе с находкой итерации 10, коммит
`8a216f3` (DECISIONS). (2) Сверка набранного тега со словарём (`tagCanon`) — мудборд,
итерация 28. (3) «Встречи и события всегда пустые» держится данными, не типом.
**Не проверено:** на устройстве нечего смотреть — экранов нет; `ZoneCache` как
поставщик пояса проверен на одной настоящей зоне (`Asia/Tomsk`).
**Риски.** Тихая потеря пункта из таблицы. Лечится скриптом сверки: вынимает
таблицы из беты и сравнивает состав.
**Тесты.** Наложения (полный перебор взаимных положений двух съёмок), сроки по
всем жанрам, доход по трём механикам оплаты, фаза события на границах.
**Checkpoint.** `migration: доменная модель съёмки`
**Сложность.** В
```
Recommended: Sonnet
Escalate to Opus if:
- правило в коде противоречит docs/12_CARD_ARCHITECTURE — это продуктовый
  вопрос, а не задача исполнителя.
```

---

### Итерация 12 — Хранилище и импорт живых данных ✔ закрыта

**Цель.** Сентябрьский сезон Алексея открывается в нативном приложении без
потерь. Самая дорогая по цене ошибки итерация всего плана.
**Делаем.** `actor Store`: атомарная запись снимка, папка вложений, дебаунс
сохранения. Плоский `Snapshot` с `CodingKeys` веба на всех типах `Domain`,
что уже описаны (`Session`, `Org`, `Block`, `Spot`, `Studio`, `TrashedItem`,
`DeliverySetting`, `GenrePrefs`) — ключи для них уже были в комментариях
`Session.swift` с итерации 11. Поля, которым ещё не дан тип (`shots`,
`boards`, `zones`, `notif`, `me`, `packs`, …), лежат в `Snapshot.extra`
(`JSONValue`) — читаются и пишутся как есть, до своей итерации. Незнакомый
код (жанр, вид документа, …) не роняет запись — становится `nil`/умолчанием
там же, где домен это уже обещал. Разбор дня терпит и «ГГГГ-ММ-ДД», и
прежний момент ISO (`CivilDate.snapshotString`). Экспорт и импорт архива
одним файлом — конверт `{app, format, exported, data}`, как пишет веб, и
голый дамп тоже.
**Не делаем.** Облако не трогаем.
**Расхождение с первой редакцией, записано дословно.** Формат читает только
текущий: «Принято: читается только текущий формат» (`docs/17_NATIVE_ARCHITECTURE.md`
§ 6, Алексей 19 сентября 2026 — «проект PWA закончен»; из семи миграций в
коде веба осталось две — `migrateCurrency` и `TAG_CODE`). Русские имена
ключей и шесть других миграций читателю не нужны: `migrateCurrency` уже
покрыт тем, что `Session.currency` — `Optional` с тем же смыслом («нет
своей — берётся из настроек»); `TAG_CODE` живёт в `RefTag.russianNames`
(итерация 11) для будущего чтения `shots`/`boards` (итерация 28), не здесь.
`s.refs`/`genreRefs` из C.1 карты данных устарели ещё до этого плана —
26 августа они стали `shots`/`boards` (DECISIONS, «Кадр отделён от
подборки»); в `Snapshot` это часть `extra`, не своё поле.
**Файлы.** `LightPlanData/Store/*` (`Store`, `Snapshot`, `BackupFile`,
`StoreError`); `LightPlanDomain/Records/*+Codable.swift`,
`LightPlanDomain/Rules/Delivery+Codable.swift`, `LightPlanDomain/Coding/*`
(снисходительное чтение кодов и `CodingKeys` — план ждал их в `Data`, но
`Codable`-синтез требует того же файла, где типы, а типы в `Domain`);
`LightPlanCore/Snapshot/*` (`JSONValue`, `SnapshotDate`,
`CivilDate+SnapshotCoding`).
**Зависит от.** 11
**Результат.** Экспорт из PWA открывается нативно, всё на месте.
**Готово, когда.** Круговой прогон (сохранил → прочитал → совпало) точен для
записей, сумм, документов и корзины; повреждённый файл не роняет приложение
(`StoreError`, не `fatalError`), а обрезанный и файл без ключа `sessions`
различаются от порчи JSON; импорт читает источник и не пишет его; резервная
копия старого снимка создаётся автоматически перед перезаписью, и если она
не вышла — импорт отказывает, а не идёт дальше без страховки. Настоящий
экспорт с телефона Алексея этим не проверен — нужен файл с него.
**Риски.** Потеря живых данных. Лечится тремя вещами: импорт только копирует;
тест на настоящем файле (**открыт** — нужен файл с телефона); резервная
копия делается до импорта автоматически.
**Тесты.** Круговой прогон; файл на 1 000 съёмок; обрезанные и повреждённые
файлы; незнакомый код не бросает; деньги (`Decimal`) без потерь — нашёлся и
исправлен настоящий баг: `Store.load` заворачивал числа в `JSONValue.number`
(`Double`) по пути к определению формы файла, что теряло точность точнее
двоичной дроби; конверт `{data:…}` и голый дамп.
**Checkpoint.** `migration: хранилище и импорт из PWA`
**Сложность.** В
```
Recommended: Sonnet
Escalate to Opus if:
- в старых данных находится случай, который не ложится в новую схему.
```

---

### Итерация 13 — Локация ✔ закрыта ∥

**Цель.** Место и его имя.
**Делаем.** `CoreLocation` по запросу, `CLGeocoder`, правила выбора и чистки
имени из веба, ручной ввод координат, сохранённые точки (совпадение 60 м),
часовой пояс места.
**Не делаем.** Карту не рисуем.
**Файлы.** `LightPlanData/Location/*`.
**Зависит от.** 11
**Результат.** «Где мы» отвечается нативно.
**Готово, когда.** Отказ в разрешении не ломает ничего — место остаётся
последним сохранённым; имена совпадают с вебом на десяти проверочных точках.
**Итог 21 сентября 2026.** Коммит `2c7bf4b` в `light_plan:native/`, ветка
`wt/iter13`, **не запушен** (пуш заблокирован разрешениями сессии, ждёт
Алексея). Слой Data: `GeoCoordinate` (подпись, ключ имени, допуск 60 м),
`PlaceNameRules` (чистка и выбор имени), `PlaceNamer` (кэш, одна повторная
попытка, отмена устаревшего), `AppleReverseGeocoder` за протоколом
`ReverseGeocoding`, `CoreLocationProvider` (одна точка по запросу),
`ManualCoordinates`, `SavedPoints` (поиск по допуску и нумерация имён),
`ZoneEstimate` и `ZoneCache`, `CurrentPlace` — «где мы» одной моделью. Сверка с
бетой — отдельным файлом `Fixtures/location.json` (1 312 проб, режется по
якорям `Tools/parity/location.js`): чистка имени, допуск, подпись координатами,
оценка пояса по долготе, ключ кэша зон, нумерация имён — совпало всё. Десять
проверочных точек замерены на настоящем `CLGeocoder` (Mac, ru_RU), ответы
записаны в тест: город из ответа и город, который дал бы веб для той же
строки, совпали на всех десяти. Тесты способны падать: три порчи (допуск
0,0007, округление, «пять и больше») дали 94 срабатывания. 27 тестов Data,
`xcodebuild test` через `LightPlan-macOS` и сборка `LightPlan-iOS` зелёные,
границы слоёв в порядке, ключ запроса разрешения виден в Info.plist сборки.
Пять уточнений по дороге. (1) **Итерация вышла раньше итерации 11**: план
ставил «зависит от 11» ради типа `Spot`, но поиск закладки по допуску от типа не
зависит — введён протокол `Located`, `Spot` в итерации 11 просто встаёт на него.
(2) **Правило «имя равно региону — это область» не перенесено**: у Apple нет
справки, по которой веб отличает столицу (`isCity`), а замер показал, что
правило убило бы Москву, Берлин, Севастополь и Петербург — у всех регион зовётся
как город. `locality` у Apple — населённый пункт по построению. (3) **Пустой
ответ геокодера — окончательный**, а не сбой: у веба он гнал вопрос ко второму
источнику, здесь источник один, а повтор ради того же пустого ответа жёг бы
лимит Apple (~50 запросов в минуту). (4) **Разбора ручного ввода в вебе нет** —
он новый: запятая и точка, буквы N/S/E/W, «°»; всё остальное отвергается с
названием причины. (5) **Общий стенд не тронут**: 9а, 14 и 15 шли рядом и правят
те же фикстуры; свой эталон — свой отпечаток.
**Не проверено:** `CLLocationManager` и системный запрос разрешения на
устройстве и в симуляторе не гонялись — только заглушка, и отказ, ограничение и
«нет сигнала» оставляют место прежним (тесты). Проверка — при экране «Где мы»
(19/21).
**Риски.** `CLGeocoder` даёт другую подробность, чем Nominatim. Лечится сверкой
и правкой правил, а не отказом от правил.
**Тесты.** Правила чистки имени — юнит-тесты на записанных ответах.
**Checkpoint.** `migration: локация и геокодер`
**Сложность.** С
```
Recommended: Sonnet
Escalate to Opus if: —
```

---

### Итерация 14 — Словарь и даты ✔ закрыта ∥

**Цель.** Строки и даты без потерь.
**Делаем.** `Tools/lang2xcstrings.js`, `Localizable.xcstrings` на пять языков
плюс варианты `en-US` и `en-GB` (каталог умеет региональные сам, вручную их
разводить не нужно), порт `datecheck.js` и `langcheck.js` в XCTest, форматы
12/24, единицы, валюты.
**Не делаем.** Не переводим то, что не переведено: `es` и `ja` остаются
заготовками с правилами числа. `ru`, `en` и `zh` заполнены и переезжают
целиком; испанский, если он к этому моменту закрыт на вебе, приезжает тем же
генератором без правки кода.
**Файлы.** `LightPlanUI/Resources/*`, `Tools/lang2xcstrings.js`.
**Зависит от.** 2
**Результат.** Ключи те же, склонение работает языком, а не строкой.
**Готово, когда.** 2 376 проверок дат зелёные; пропавший ключ падает в
английский, а не показывает сам ключ; все ключи из `lang.js` доехали —
сверяется числом: `ru`, `en` и `zh` должны дать одинаковый состав.
**Итог 21 сентября 2026.** Коммит `f365e02` в `light_plan:native/`, запушен.
Каталог `Localizable.xcstrings` собирает `Tools/lang2xcstrings.js` из
`beta/lang.js`: **1 542 ключа**, из них 31 склоняемый, языки `ru en en-US es ja zh`
(`en-GB` пуст — цепочка сама ведёт к основе; 1,3 МБ). К моменту работы `es` и
`ja` уже не заготовки, а полные словари (см. `docs/17`, § 13), поэтому генератор
взял все пять без правки кода; строка плана «`es` и `ja` — заготовки» устарела.
В Swift — `Lexicon` (цепочка говор → основа → английский → сам ключ, `t`, `word`,
`count`, `sep`, подстановка `{name}`), `DateText`, `ClockText`, `NumberText`,
`Temperature` в `LightPlanUI/Text/`. Эталон — `Fixtures/lang.json` и
`format.json`, их считает `Tools/parity/lang.js` (`make lang`), отдельно от
стенда неба: общий `BLOCKS` не тронут, отпечаток первых восьми файлов не сдвинулся.
Сверено с бетой: строки **10 794** (1 542 ключа × 7 словарей), склонение —
7 словарей × 31 слово × 30 чисел (слово, «число со словом», разделитель, номер
формы), подстановка по имени, даты **22 170** (все дни 2026-го + 29.02.2028 и
31.12.2031 × 6 языков × 10 видов + шапка недели и 12 месяцев), порт `datecheck.js`
**2 376** из 2 376, часы **3 330** (6 языков × 3 настройки × 28 моментов
и 100 промежутков), число и деньги **666**, градусы. Порт `langcheck.js`:
`ru`, `en`, `es`, `ja`, `zh` — по 1 542 ключа, `en-US` — 5 отличий, `en-GB` — 0,
без сирот. Пропавший ключ падает в английский — проверено на маленьком тестовом
каталоге (настоящий полон, в нём пропажу не показать). Тесты способны падать:
сдвиг границы русского «2–4» ломает склонение (62 расхождения), четыре знака
вместо трёх в коротком месяце — даты (515 и 678), снятие «−0» — деньги (72),
слово в каталоге — строки. `xcodebuild test` через оба приложения (пять пакетов:
53 + 27 + 12 тестов и остальные) и сборка `LightPlan-iOS` зелёные, границы слоёв
в порядке. Работали параллельно с 9а, 13 и 15, каждый в своём worktree;
пересечений не было, rebase прошёл чисто.
Четыре уточнения по дороге. (1) **Каталог требует, чтобы форма множественного
числа ссылалась на число** («Plural variation requires referencing the number in
the string») — а веб отдаёт слово без числа. Это тот риск, что назван ниже, и он
сбылся. Форма хранится как `%lld` + невидимая метка `U+2063` + слово; `word`
берёт то, что после метки, `count` собирает число, разделитель языка и слово.
(2) **SwiftPM на хосте не компилирует каталог** (замерено: в ресурсах остаётся
сырой JSON, папок `xx.lproj` нет) — его собирает только Xcode. Тесты каталога
поэтому идут через `xcodebuild test`, а под `swift test` пропускаются с пометкой;
форматные тесты идут в обоих. (3) **Платформа склоняет по локали процесса, а не
по языку папки**, если не передать `locale:` явно — в англоязычной папке «21
shoot» на русской машине; `Lexicon` передаёт локаль слова, замер это и поймал.
(4) Язык экрана в нативе выбирается явно (`Lexicon("ru")`), как `LANG.set` в вебе,
системный язык не читается. Когда появится экран выбора языка — решать
Алексею: следовать ли настройке iOS «язык приложения» (тогда `en-US`/`en-GB`
придётся мапить на системные регионы).
**Расхождения платформы и эталона, решённые в порте** (подробно — DECISIONS,
«Форматы текста в нативе»): пробел перед AM/PM (U+202F у платформы, U+00A0 в
`fmt` веба — порт держит U+00A0 везде); длинное тире в русском 12-часовом
промежутке (порт — короткое, как в 24-часовой ветке); японское одиночное время
«午前12:00» (порт повторяет веб, платформа пишет «午前0:00»). Не решено, а
принято как известное: русский 12-часовой промежуток через полдень — веб ставит
пробелы вокруг тире, платформа нет (40 случаев из 3 330, в тесте — узкое
исключение со счётчиком).
**Найдено в вебе, не чинить (паритет дороже):** `money(-0.4)` даёт «-0 ₽»
(`Math.round` → −0); `fmt(1439.5)` даёт «24:00» (округление до 1440, а не
замыкание суток); японские часы в одном и том же режиме пишут «午前12:00» одним
видом и «午前0時00分～7時00分» другим. Все три повторены в Swift и лежат в фикстурах.
**Не проверено:** эталон — `Intl` узла (ICU 78, CLDR 48), а не Safari на телефоне
Алексея; что покажет сам iOS, сверять при экранах с датами (итерации 21+). Скорость
`Lexicon` отдельно не мерена.
**Риски.** Плюрализация каталога не покроет случай из `LANG.word` — правится
точечно, с записью в решения.
**Тесты.** Порт `datecheck.js`; порт `langcheck.js`.
**Checkpoint.** `migration: словарь и даты`
**Сложность.** С
```
Recommended: Sonnet
Escalate to Opus if: —
```

---

### Итерация 15 — Знаки ✔ закрыта ∥

**Цель.** Библиотека знаков в нативных ресурсах.
**Делаем.** `Tools/icons2assets.js`, контуры знаков в Swift (не asset catalog —
см. итог), `Icon.swift` с теми же именами, правило подбора знака точки по слову
(`POINT_WORDS`).
**Не делаем.** Не рисуем новых знаков, не подменяем SF Symbols.
**Файлы.** `LightPlanUI/Icons/*`.
**Зависит от.** 2
**Результат.** Любой знак доступен по имени, как `IC.svg(...)` сегодня.
**Готово, когда.** Контрольный лист всех знаков рендерится в обеих темах, одна
толщина линии на весь набор.
**Итог 21 сентября 2026.** Коммит `69bb5a1` в `light_plan:native/`, запушен.
Библиотека — 217 записей девяти наборов `icons.js`: в общем словаре 196 имён
(первый набор выигрывает, как `merge` в вебе; `signs.rings` перекрыт `points.rings`
и мёртв), жанры 12, пожелания 8 — свои пространства имён. Генератор
`Tools/icons2assets.js` читает живой `beta/icons.js` и сверяет его со слепком в
`beta/index.html` побайтно (90 850 знаков, на 21.09 равны; разошлись — стоп).
Каждое тело раскладывается на 220 частей и 4 697 команд `M L C Q Z`: дуги,
`H`/`V`/`S`, относительные команды и примитивы сведены в JS, Swift только
проигрывает. В Swift: `Icon("имя", size:, line:)`, `Icon(genre:)`, `Icon(wish:)`,
`Icon(point:place:studio:)`, `PointSign` (166 правил `POINT_WORDS`, чистый
Foundation — при нужде переезжает в Domain без правки), `IconSheet` — контрольный
лист. Цвет — `.foregroundStyle` снаружи, у погоды части красятся врозь
(`partStyles`), как классы `i-sun`, `i-cloud`, `i-rain` в CSS. `make icons`.
**Хранение изменено против § 4.10 и `docs/17` § 14: контуры, не asset catalog.**
Причина — из кода, каталог не мерили: веб задаёт толщину линии снаружи (в
`beta/index.html` `stroke-width` от 1,4 до 2,4 по контекстам — 1,6 у 34 правил,
1,5 у 25, 1,8 у 10), а погода красит части по отдельности; ресурс каталога
запекает и то, и другое. Строка «Вектор в asset catalog масштабируется иначе»
осталась непроверенной гипотезой, потому что каталог не понадобился.
«Одна толщина линии на весь набор» — свойство листа (1,5 на холсте 24×24, у
листа веба то же), а не знаков: своя толщина у единственной части во всей
библиотеке — клетки флага финиша (4,4, плоские концы), это проверяет тест.
Сверено: (1) **визуально с Chromium** — лист 216 клеток по 96 px рисуют Chromium
из `icons.js` и Swift `Icon`-ом; пересечение тёмных пикселей 0,925–0,995
(среднее 0,971), чернил 0,98–1,03 от эталона, тёмных пикселей дальше 2 px от
пары не больше 0,07 % — разница только в сглаживании. Цифры на macOS и в
симуляторе совпали до знака. (2) **Подбор знака** — 18 013 названий, собранных
из самих правил (каждая альтернатива регулярки в шести обрамлениях, места,
студия): все совпали с `ICONS.pointSign`. Тесты способны падать: подстановка
наивного `\b` вместо явных границ роняет 1 754 названия; первый замер цвета
поймал настоящую ошибку — часть знака красилась `.foreground`, тот
разрешается в `.primary` (чёрный при 85 %) и перекрывал цвет вызывающего, чернил
было 0,84 от эталона. `xcodebuild test` через `LightPlan-iOS` зелёный (53 + 25
тестов пакета UI и остальные), границы слоёв в порядке, `make icons`
воспроизводится побайтно. Работали параллельно с 9а, 13 и 14, каждый в своём
worktree; rebase дал конфликт в `Makefile` и `Package.swift` (14 добавила туда
каталог и ресурсы), решён сложением.
**Найдено, не сделано.** (1) Два экземпляра знаков в вебе (`icons.js` и слепок
в `index.html`) — не хвост, а устройство веба: пункт ROADMAP «отвязать
библиотеку знаков» закрыт встраиванием слепка ещё до миграции (ROADMAP,
«Ближайшее», пункт 1; поправка 21 сентября 2026 — сначала здесь было записано
«не закрыт»). Генератор ловит расхождение двух экземпляров, этого достаточно.
(2) Слово,
которое знака не даёт, идёт по ярусам как в вебе; писать новые знаки —
не эта итерация. (3) Разбор SVG-путей в генераторе знает `M L H V C S Q T A Z`,
библиотека пользуется всеми, кроме `Q` и `T`; другой атрибут или тег в новом знаке
генератор отвергнет с именем знака, а не пропустит.
**Риски.** Вектор в asset catalog масштабируется иначе, чем SVG в браузере,
особенно обводка. Лечится контрольным листом, а не догадками.
**Тесты.** Snapshot контрольного листа.
**Checkpoint.** `migration: библиотека знаков`
**Сложность.** Н
```
Recommended: Sonnet
Escalate to Opus if:
- обводки едут и нужен другой способ хранения (Shape вместо SVG).
```

---

### Итерация 16 — Временная модель ✔ закрыта

**Цель.** Правильная модель времени, отдельно от экрана.
**Делаем.** `LightPlanTimeline`: выбранный день, минута, окно суток, шаг,
положение ленты и барабана, перецентровка на границе, «сейчас», направление
перехода (`dayShift`), машина взвода и срыва с константами из веба.
**Не делаем.** Ни одного `View`.
**Файлы.** `LightPlanTimeline/*`.
**Зависит от.** 7
**Результат.** Механика времени тестируется без экрана.
**Готово, когда.** «Довёл до упора и держал 1.06 с» даёт ровно один переход;
«довёл и отпустил через 0.5 с» — ни одного; при перецентровке метка не
смещается, и это проверяется числом, а не глазом.
**Итог 22 сентября 2026.** Коммит `911448d` в `light_plan:native/`, запушен.
`Transmission` — порт `Spikes/TimebarSpike/App/Transmission.swift` (итерация 3)
без изменений констант: `WIND_NEED`/`need` 34, `DWELL_RATE`/`dwellRate` 32,
`STRAIN_MAX`/`strainMax` 6, `EDGE_ZONE`/`edgeZone` 28, стравливание 0,2 с.
`TimelineMachine` держит выбранный день (`CivilDate`), минуту, окно суток
(`SolarDay.mint…maxt` того же дня и места — пересчитывается заново при каждом
переходе, как веб зовёт `computeSun(selDate)` внутри `ribbonDayFlip`), ленту и
барабан, `dayShift`, потребляемый ровно один раз (`consumeDayShift`, как веб
читает и сам обнуляет `dayShift`). Переход через сутки — общей арифметикой
`CivilDate.adding(days:)` итерации 10, без своего календаря: границы месяца,
года и 29 февраля проверены тестами через неё, а не заново.
**Упрощение против веба, записанное как решение исполнителя.** Веб ведёт
ползунок по `MINT…MAXT` (солнечная полночь), а ленту — по плоской шкале
0…1440 с клампом в `MINT…MAXT` только при записи (`ribbonApply`); здесь одно
окно на оба контрола. Расхождение — секунды на долготах, где солнечный
полдень не совпадает с 12:00: не замысел, а неровность веба (корзина 2, § A.4
плана), и после `SolarDay` (итерация 7) её незачем тащить дальше.
**Найдено при переносе.** `Transmission.dwell` не выставляет `spent` сама —
это подтверждает вызывающий через `fired()`, иначе `.fire` возвращается на
каждом кадре, пока палец лежит на упоре (поймано первым же заходом теста
«держали 1,1 с» — 137 срывов вместо одного). Контракт соблюдён в
`TimelineMachine.fireDay`, а негативный случай (без вызова `fired()`) оставлен
тестом-документацией в `TransmissionTests`, чтобы будущий вызывающий (UI
итерации 17) не наступил на то же.
**Не решался вопрос «сейчас».** `jumpToNow(day:minute:)` принимает уже
посчитанные вызывающим день и минуту (`WallTime(moment:utcOffsetHours:)` в
зоне места), а не трогает `Date()` сама — потому что смещение зоны зависит от
даты (переход часов), и разрешать эту курицу-и-яйцо внутри чистой машины
дороже, чем один раз посчитать снаружи.
16 тестов: срыв ровно один раз при удержании 1,06 с и ноль при отпускании на
0,5 с (сырой `Transmission` и через полный `TimelineMachine.dragSlider`),
переход через месяц/год вперёд и назад, барабан не трогает время суток,
перецентровка ленты не даёт скрытого дрейфа (независимая от пути сборка даёт
тот же `ribbonOffset`), «сейчас» с сегодняшним и другим днём. `swift test`
пакета — 16/16; `xcodebuild test` зелёный на `LightPlan-iOS` и `LightPlan-macOS`;
`Tools/check_boundaries.sh` — 5 пакетов, 67 файлов, ок.
**Риски.** Соблазн подогнать константы на глаз — они откалиброваны на живом
телефоне и записаны в `DECISIONS.md`.
**Тесты.** Сценарии жестов последовательностями событий; перецентровка; переход
через полночь, через первое число, через год.
**Checkpoint.** `migration: временная модель`
**Сложность.** В
```
Recommended: Sonnet
Escalate to Opus if:
- модель не выражается без ссылки на пиксели экрана;
- поведение на границе суток расходится с DECISIONS.
```

---

### Итерация 17 — Таймбар в приложении ✔ закрыта

**22 сентября 2026.** Коммит `6ee85ae` в `light_plan:native/`, запушен.

**Цель.** Перенести прототип 3 в продукт и подключить к настоящим данным.
**Делаем.** Ползунок, ярус, риски, дорожка света от `SolarDay`, лента и барабан
с настоящими датами, знаками погоды и фазами луны, «↺ сейчас», края с восходом
и закатом, отдача на детентах, звук срыва.
**Не делаем.** Не переизобретаем механику — она решена в итерации 3.
**Файлы.** `LightPlanUI/Timebar/*`.
**Зависит от.** 3, 10, 15, 16
**Результат.** Время управляется как в вебе.
**Готово, когда.** Снимок совпадает с вебом; барабан не рисует погодный знак
там, где нет настоящего прогноза (инвариант 11); 60 кадров.
**Итог.** `TimebarState` — обёртка над чистой `TimelineMachine` (итерация 16):
добавляет то, что план сознательно отнёс к UI, — анимацию срыва и доезжания
барабана, отдачу, звук, дорожку света, знаки погоды и луны, кнопку «сейчас».
`ScrubView`/`RibbonView`/`RulerView` — порт `Spikes/TimebarSpike` (итерация 3)
без изменения чисел передачи разряда (порог 34, скорость удержания 32, зона
края 28 pt и так далее). `RootView` — временный хозяин экрана на настоящих
данных (Барнаул, `Open-Meteo`) до экрана «Свет» (итерация 19).

Три решения исполнителя, каждое — реализация другая при том же замысле
(корзина 2 плана, § A.4), а не отступление от него:
- **Дорожка света** красится готовой `LightPalette.skyColor(elevation:)`
  (итерация 8), а не веб-таблицей `PATH_STOPS` — та кормит путь солнца на
  куполе и карте (итерации 18, 20), которых ещё нет; второй таблицы того же
  смысла заводить незачем, сводить их можно будет, когда купол появится.
- **Погодные знаки барабана** взяты из библиотеки `icons.js` (`clear`,
  `cloud`, `rain`, `fog`, итерация 15) вместо отдельного порта вебовской
  функции `skyIcon` — та рисует те же четыре знака в обход общей библиотеки;
  ставить рядом второй, отдельно поддерживаемый набор путей ради знака в
  14 pt было бы прямым нарушением правила «сперва лист знаков» (`ws:CLAUDE.md`).
- **Фаза луны в ячейке** — свой `Shape` (`MoonGlyph`, порт формулы `litPath`
  точками по параметру), а не путь из `IconArt`: тот разбирает только прямые
  и кривые Безье, эллиптическая дуга `litPath` в них не сведена.

Тем же заходом `TimelineMachine` (итерация 16) получил три маленьких прохода,
которые сама и отложила: `bleedWind`/`resetWind` для стравливания (итерация 16
писала в комментарии — «анимация... дело UI, итерация 17») и именованная
`drumSpan` вместо инлайновой четвёрки в двух местах. Проверено теми же 16
тестами модуля, ни один не тронут.

**Найдено при переносе.** `CADisplayLink(target:selector:)` недоступен на
macOS-таргете (там свой путь через `NSScreen`) — обёрнуто в `FrameTicker`:
`CADisplayLink` на iOS как есть, `Timer` на 60 Гц на macOS, где точность кадра
не критична (трекпад — не палец на стекле). `TimebarHaptics` — только iOS
(Taptic Engine), на macOS пустая заглушка с тем же интерфейсом: барабан и
слайдер работают, отдачи не показывая.

**Проверено.** `xcodebuild build` и `test` зелёные на обеих схемах: 5 из 5
пакетов, 177 тестов (60+41+16+35+25), 0 падений что на iOS, что на macOS.
`Tools/check_boundaries.sh` — 5 пакетов, 78 файлов, ок. На симуляторе видно
живые данные — настоящий восход/закат Барнаула, настоящий прогноз Open-Meteo
разными знаками по дням (инвариант 11 держится: там, где `WeatherStore`
ещё не ответил, ячейка пуста).

**Не проверено в эту сессию.** Снимок с вебом (`tools/shot.js`) не снят —
стенда для таймбара отдельно от купола ещё нет, а без купола сравнивать
нечего пиксель в пиксель; сценарий срыва не пойман на живом жесте — попытки
через синтетический тач симулятора не довели касание до `DragGesture`
(инструмент, не код: то же самое число `xcrun simctl`-жестов молчало и на
пустом экране). Числа передачи разряда не менялись с итерации 3, где они уже
подтверждены на телефоне (DECISIONS «Отдача таймбара», «Барабан в нативе») —
но саму сборку на настоящем жесте стоит подержать в руках ещё раз.
**Подтверждено на телефоне 22 сентября 2026** (первая полная сборка на
устройстве, Алексей): «жест работает штатно» — DECISIONS, «Жест среза
подтверждён на телефоне».
**Риски.** Разрыв между прототипом и продуктом — лечится тем, что прототип
переносится целиком, а не переписывается.
**Тесты.** Snapshot двух видов ленты; сценарий срыва на устройстве.
**Checkpoint.** `migration: таймбар`
**Сложность.** В
```
Recommended: Sonnet
Escalate to Opus if:
- при подключении настоящих данных механика ведёт себя иначе, чем в прототипе.
```

---

### Итерация 18 — Купол ✔ закрыта

**22 сентября 2026.** Коммит `74a1fed` в `light_plan:native/`, запушен.

**Цель.** Главный прибор экрана «Свет».
**Делаем.** `Canvas`: дуга дня градиентом по модели света, светило с плоским
диском и обозначенной кромкой, тень как свойство слоя, зарево у горизонта, свет
ушедшего светила, линия горизонта, звёзды, метеоры, кольцо «сейчас», лунный диск
с фазой, тумблер солнце/луна.
**Не делаем.** Композицию и пропорции не меняем.
**Файлы.** `LightPlanUI/Dome/*`.
**Зависит от.** 8, 9, 15, 17
**Результат.** Купол живёт и следует за временем. Веб переходит в состояние
«эталон».
**Готово, когда.** Снимок совпадает с `node tools/shot.js` по положению светила,
углу дуги и цветам на восьми моментах суток в обеих темах.
**Итог.** `LightPlanUI/Dome/*` — один `Canvas` в модельных координатах веба
(`DomeGeometry`: `cx=195, cy=196, rx=163, ry=148`, `viewBox` 390×240), а не
дерево `View`/`Shape` на каждую деталь: план прямо назвал холст. `DomeGeometry`
— порт `posOn`/`nightAmp`/`polarAt`/`posAt`/`deepOf`/`shadeNeed` в чистых
функциях над `SolarDay`, испытан отдельно от рисования. `DomeStars` — 280 точек
`mulberry32(20260715)`, тот же посев и порядок вызовов `rnd()`, что у веба.
`DomeMeteor`/`DomeMeteorController` — пасхалка «тишина под звёздами» (веб,
`spawnMeteor`): диапазоны перенесены, сам момент падения — нет, `Math.random`
там и в вебе не для физики продукта. `SkySwapButton` — тумблер солнце/луна,
делит `showMoon` с `TimebarState` (итерация 17 оставила это поле куполу).
`DomeView` подключён к `RootView` тем же временным способом, что таймбар.

Три решения исполнителя, каждое — реализация другая при том же замысле, а не
отступление от него:
- **Тень светила — свойство слоя (`GraphicsContext` `.shadow` у `drawLayer`),
  а не радиальный градиент.** Ровно риск, который план предупредил записать:
  мягкость другая, сила — по той же `shadeNeed` (WCAG-разрыв со светлым
  фоном). Числа проверены вручную по комментарию веба: `shadeNeed(silver)`
  (полдень) = 1, `shadeNeed(gold)` (золотой час) = 0 — те самые 0,11 и 0,51
  разрыва светлоты, что называет сам веб в комментарии у формулы.
- **Дуга дня — статичный градиент, перенесён буквально, не «по смыслу».**
  `#dayLight`/`#moonLight` веба — фиксированные стопы, не пересчитываются по
  минуте: сама разметка расходится с описанием задачи «градиентом по модели
  света», и буквальный перенос (`ws:CLAUDE.md`) важнее домысленного.
- **Знак луны в тумблере — тот же `MoonGlyph`**, что в ячейке барабана
  (итерация 17), а не отдельная кривая: второй контур ради значка 12×12 pt
  нарушал бы то же правило, что уже остановило отдельный `skyIcon` в 17.

**Найдено, не сделано.** Зарево у горизонта не берёт погоду: веб подмешивает
`skyPalette(curWeather)` (`SKY_STOPS`, `mixDead`) при `e < 8`, но итерация 10
эту функцию не переносила — не значилась в её «Делаем». Зарево красится только
моделью света (`LightState.color`/`.glow`). Разница видна лишь у самого
горизонта в облачную погоду. Подробности и предложение — DECISIONS «Купол в
Swift».

**Проверено.** `xcodebuild build` и `test` зелёные на обеих схемах: 5 из 5
пакетов, 184 теста (было 177, +7 геометрии купола), 0 падений что на iOS, что
на macOS. `Tools/check_boundaries.sh` — 5 пакетов, 84 файла, ок. На симуляторе
глазами (Барнаул, обе темы через `.preferredColorScheme`): дуга, диск с
кромкой и зарево, кольцо «сейчас», переключение солнце/луна — тумблер купола
сразу меняет знаки барабана таймбара (общее поле `showMoon`), значит связка
работает в обе стороны.

**Не проверено в эту сессию.** Снимок с вебом (`node tools/shot.js`) не снят —
у инструмента нет команды встать на экран «Сегодня» и прочитать дугу, диск и
цвета так, как он уже умеет для карты; отдельного стенда для купола ещё нет.
Готовность по формальному критерию плана («восемь моментов суток, обе темы»)
остаётся непроверенной числами — то же ограничение, что таймбар (итерация 17)
оставил после себя нетронутым: без снимка сравнивать нечего пиксель в пиксель.
Синтетический жест на тумблере сработал (переключение подтверждено
скриншотом), драг ленты слайдера через симулятор — нет, тем же ограничением
инструмента, что и в итерации 17.
**Подтверждено на телефоне 22 сентября 2026** — тот же замер, что закрыл
драг у итерации 17: «жест работает штатно» (DECISIONS, «Жест среза
подтверждён на телефоне»).
**Риски.** Тень как свойство слоя даст иную мягкость, чем радиальный градиент.
Это допустимое улучшение (ROADMAP на него рассчитывает), но оно должно быть
замечено и записано, а не пройти незаметно.
**Тесты.** Snapshot: 8 моментов × 2 темы × 2 светила.
**Checkpoint.** `migration: купол`
**Сложность.** В
```
Recommended: Sonnet
Escalate to Opus if:
- цвета не сходятся и вопрос упирается в цветовое пространство;
- производительность падает при движении ползунка.
```

---

### Итерация 19 — Экран «Свет» ✔ закрыта

**22 сентября 2026.** Коммит `d805765` в `light_plan:native/`, запушен.

**Цель.** Первый настоящий экран.
**Делаем.** Шапка, купол, показание, «что дальше», спарклайн, телеметрия семью
строками, спойлер «Подробно», кнопка съёмки, режим Просто/Астро, тема.
**Не делаем.** Планировщика не касаемся (инвариант 13).
**Файлы.** `LightPlanUI/Screens/Light/*`.
**Зависит от.** 10, 18
**Результат.** Экран работает от живых данных.
**Готово, когда.** Семь строк телеметрии совпадают с вебом на пяти проверочных
днях; Просто/Астро убирает ровно то же, что в вебе.
**Итог.** `LightPlanUI/Screens/Light/*` — пять файлов: `LightTelemetry`
(чистая модель, порт `renderToday`/`renderPro` без DOM и SwiftUI),
`LightScreenModel` (`@Observable`, держит `TimebarState` и `WeatherStore`,
считает телеметрию на минуту), `LightScreenView` (композиция), `LightSpoilerView`
(разворот «Подробно»), `SparklineShape` (порт `sparkline(vals)` как `Shape`).
`WeatherStore` получил `airSample(for:hour:)`, `TimebarState` — `todayInPlace`
(оба — маленькие публичные обёртки над уже существующей логикой, не новая
математика). `RootView` отдал композицию экрану вместо голых купола и
таймбара, как и обещал его комментарий с итерации 17.

Одно решение исполнителя стоит записи отдельно от DECISIONS (там — полная
версия с обоснованием): Просто/Астро (`proMode`) — временное поле нового
`LightScreenModel`, а не третье поле в `TimebarState` рядом с `showMoon`:
делят состояние только те узлы, что физически делят экран (купол и барабан),
а переключатель спойлера принадлежит только этому экрану.

**Найдено, не сделано.** Прогноз-палитра зарева заката (`.sky-swatch`,
`skyPalette`/`SKY_STOPS`) — декоративный градиент под группой «Прогноз
заката», не число; сами числа группы (балл, три яруса, влажность) перенесены.
Столбчатый значок кнопки «Подробно» (веб рисует его в разметке, не из
`icons.js`) — заменён текстом с шевроном, в библиотеке знаков (итерация 15)
такого глифа нет. Ночной глиф (веб красит его сплошь) идёт тем же контурным
начертанием, что остальные строки. Единица температуры всегда «C» —
переключателя °C/°F ещё нет (итерация 19а). Подробности и предложения —
DECISIONS «Экран «Свет» в Swift».

**Проверено.** `xcodebuild build` и `test` зелёные на обеих схемах: 5 из 5
пакетов, 192 теста (было 184, +8 на телеметрию: поправка непогоды на
экспонометр даже ночью, три порога цвета закатной строки, слово тренда
облачности, состав строки ветра (направление и порывы не всегда в строке),
16 румбов по азимуту, луна первой группой спойлера только в лунном режиме,
«белая ночь» вместо прочерка на широте без −18°, геометрия спарклайна),
0 падений. `Tools/check_boundaries.sh` — 5 пакетов, 90 файлов, ок. `make lang`
подтвердил каталог уже полным (все ключи для экрана были в `beta/lang.js`
заранее, ни одной новой строки не потребовалось). На симуляторе глазами
(Барнаул, настоящий прогноз Open-Meteo — в момент проверки дождь): шапка,
купол с показанием, «что дальше» со спарклайном, семь строк телеметрии,
включая живое срабатывание «непогода топит экспонометр» (деления вместо
звёзд, цвет тухнет), кнопка съёмки с верным подзаголовком.

**Не проверено в эту сессию.** Снимок с вебом (`node tools/shot.js`) не снят
— та же причина, что и у купола (итерация 18): у инструмента нет команды
встать на экран «Сегодня», только на карту. Формальный критерий плана
(«семь строк совпадают с вебом на пяти датах») поэтому не сверен построчно
числами веба — сверены числами составные правила сборки строк (пороги,
слова, состав), а математика внутри (солнце, луна, погода, закатный балл)
уже стоит на стенде паритета итераций 7–11 и в эту сессию не менялась.
Спойлер открыт/закрыт не переснят глазами (код испытан числами на составе
групп). Жест на тумблере солнце/луна синтетическим тачем симулятора не
подтверждён — то же ограничение инструмента, что итерации 17 и 18 уже
оставили нетронутым.
**Подтверждено на телефоне 22 сентября 2026** — первая полная сборка
`LightPlan-iOS` поставлена на устройство через Xcode/`devicectl`, Алексей
подтвердил: «жест работает штатно» (DECISIONS, «Жест среза подтверждён на
телефоне»).
**Риски.** Порядок и вес строк — часть иерархии решения; сверять по
`10_SCREEN_SPECIFICATION`, а не по вкусу.
**Тесты.** Snapshot экрана; сверка чисел телеметрии.
**Checkpoint.** `migration: экран Свет`
**Сложность.** С
```
Recommended: Sonnet
Escalate to Opus if: —
```

---

### Итерация 19а — Экран «Настройки»: каркас и свои разделы ∥

**Цель.** Экран, которого план не назначал ни одной итерации (найдено
21 сентября 2026 при вопросе о языке): в вебе это `s-set`, 22 раздела в семи
главах, по слепку плана 635 строк, 5 % веба.
**Делаем.** Каркас экрана с семью главами в порядке веба и разделы, которым
не нужна чужая итерация: режим Просто/Астро, вид прорези барабана, тема, шаг
времени, порог дороги, единицы, валюта, практика ведения дел, обратная связь,
номер сборки. **Язык — смешанным способом** (DECISIONS, «Язык приложения —
из iOS, строка в настройках ведёт туда»): язык берётся из настройки iOS для
приложения, строка «Язык» открывает эту страницу iPhone; британский или
американский английский — по региону телефона. **Город по умолчанию** по
решению 19 сентября (ROADMAP, «Город по умолчанию»): город настроек →
место пользователя, если доступ уже дан → ближайшая столица по поясу среди
стран языка телефона; вопрос «Откуда вы работаете» при первом запуске.
**Не делаем.** Разделы, чьё содержимое делает другая итерация, — они
добавляют свою строку сами: корзина (22), слои и подписи карты и источник
карты (20), мудборды и организации (28), знакомство (29), уведомления (30),
копия данных и облако (31).
**Файлы.** `LightPlanUI/Screens/Settings/*`.
**Зависит от.** 13, 14, 19
**Результат.** Настройки открываются, свои разделы работают, у чужих —
место в своей главе.
**Готово, когда.** Главы и порядок разделов совпадают с вебом; смена темы,
режима и шага доезжает до «Света» без перезапуска; строка «Язык» открывает
страницу приложения в настройках iOS.
**Риски.** Смена языка в iOS перезапускает приложение — проверить, что
несохранённое не теряется.
**Тесты.** Snapshot экрана в обеих темах; город по умолчанию на трёх случаях
(город задан, доступ к геолокации дан, не дан).
**Checkpoint.** `migration: экран Настройки`
**Сложность.** С
```
Recommended: Sonnet
Escalate to Opus if: —
```

---

### Итерация 20 — Экран «Карта»

**Цель.** Карта как прибор.
**Делаем.** Холст выбранным в итерации 4 путём; прибор поверх: лимб, деления,
восемь чисел азимута, две оси, компас-роза, пояс рабочей высоты, путь солнца с
часовыми засечками, луна, три яруса точек МП, трек ядра, чип азимута, ночная
вуаль, ротор с живым компасом, меню слоёв, сворачиваемая сводка, сохранённые
точки. В сводке — пять строк Млечного Пути: темнота (10), луна (9а), засветка,
погода над окном (10), вердикт. Засветка — атлас Лоренца: загрузка плиток,
распаковка gzip, `glowIndex`/`glowRead`/`glowLevel`/`glowMag`; условия
поставщика сверяются здесь же (`docs/17` § 9). Атлас назначен явно 21 сентября:
прежде он был только в таблице сервисов архитектуры.
**Не делаем.** Не пересматриваем решение по холсту.
**Файлы.** `LightPlanUI/Screens/Map/*`.
**Зависит от.** 4, 9, 13, 19
**Результат.** Второй экран прибора готов.
**Готово, когда.** Прибор совпадает по геометрии с вебом на четырёх моментах;
поворот ротора не оставляет клина пустоты (диагональ кадра, 9504).
**Риски.** Производительность точек МП: 120 точек полосы пересчитываются на
каждое движение ползунка.
**Тесты.** Snapshot прибора; поворот на восьми углах; замер кадров.
**Checkpoint.** `migration: экран Карта`
**Сложность.** ОВ
```
Recommended: Sonnet
Escalate to Opus if:
- выбранный путь холста не выдерживает полного прибора поверх.
```

---

### Итерация 21 — Планировщик: месяц, неделя, день

**Цель.** Ядро календаря, на котором держится ежедневная работа.
**Делаем.** Три вида; закреплённая неделя дат; сводка дня; лента дня с часовой
осью, свободными окнами (`openGaps`), съёмками и занятым временем; три кнопки
действий; направление въезда ленты (`dayShift`).
**Не делаем.** Ни года, ни статистики, ни формы, ни карточки — заглушки.
**Файлы.** `LightPlanUI/Screens/Planner/*`.
**Зависит от.** 12, 19
**Результат.** По календарю можно жить.
**Готово, когда.** На импортированных настоящих данных три вида показывают то
же, что PWA, включая цвет срочности сдачи и точки качества дня.
**Риски.** Пять видов легко расползаются на пять состояний. Состояние должно
быть одно (`calScope`), как в вебе.
**Тесты.** Snapshot трёх видов на одном наборе; лента дня с наложениями.
**Checkpoint.** `migration: планировщик, месяц-неделя-день`
**Сложность.** ОВ
```
Recommended: Sonnet
Escalate to Opus if:
- три вида не складываются в один тип состояния и появляется дублирование.
```

---

### Итерация 22 — Планировщик: год, год-12, статистика, корзина

**Цель.** Дальний обзор и итоги.
**Делаем.** Год; год-12 сеткой месяцев с зумом в месяц; статистика сроков
сдачи; прибыль по валютам; сигнал перегруза; поиск; корзина с возвратом; свайп
влево и полоса отмены.
**Не делаем.** Не упрощаем зум год-12 → месяц молча.
**Файлы.** `LightPlanUI/Screens/Planner/*`.
**Зависит от.** 21
**Результат.** Планировщик закрыт целиком.
**Готово, когда.** Зум повторяет веб; удаление обратимо (инвариант 17);
суммы совпадают по каждой валюте.
**Риски.** Зум — самостоятельная анимация; при затруднении выносится отдельной
вехой, а не упрощается.
**Тесты.** Snapshot года и года-12; удаление и возврат; суммы на настоящих
данных.
**Checkpoint.** `migration: планировщик, год и итоги`
**Сложность.** В
```
Recommended: Sonnet
Escalate to Opus if:
- зум год-12 → месяц не выражается переходом.
```

---

### Итерация 23 — Форма: состав по жанрам, барабаны, клиент

**Цель.** Завести запись.
**Делаем.** Состав формы по жанру и группе (11 жанров, 4 группы); барабаны даты,
часа и минуты — **системным колесом**, не переносом веба (DECISIONS, «Барабаны
формы — системное колесо»): пределы через минимум и максимум колеса, шаг
15/30 минут через интервал минут; сетка выбора даты; клиент, пара, организация; телефон с
форматированием; черновик с автосохранением.
**Не делаем.** Ни маршрута, ни денег, ни документов — следующая итерация.
**Файлы.** `LightPlanUI/Screens/Form/*`.
**Зависит от.** 21
**Результат.** Запись создаётся.
**Готово, когда.** Для каждого из 11 жанров состав полей совпадает с вебом;
черновик переживает закрытие приложения.
**Риски.** Матрица полей — продуктовое решение, сверяется с
`docs/12_CARD_ARCHITECTURE`, а не с кодом при расхождении.
**Тесты.** Матрица полей по жанрам; черновик.
**Checkpoint.** `migration: форма, состав и клиент`
**Сложность.** В
```
Recommended: Sonnet
Escalate to Opus if:
- матрица полей в коде расходится с docs.
```

---

### Итерация 24 — Форма: маршрут, деньги, сдача, документы

**Цель.** Дописать запись до полной.
**Делаем.** Маршрут точек; пожелания к погоде и проверка «замысел против
прогноза»; оплата, расходы, предоплата, валюта; сдача материала; референсы,
документы, оборудование; сохранение.
**Не делаем.** Карточку не трогаем.
**Файлы.** `LightPlanUI/Screens/Form/*`.
**Зависит от.** 23
**Результат.** Запись полная и правится без потерь.
**Готово, когда.** Правка **изменяет запись**, а не собирает новую: поля, которых
нет в форме (`fromMeetId`, `questSent`, `grewToId`), сохраняются сами. Сегодня в
вебе их переносят руками (16424) — здесь класс ошибки должен исчезнуть.
**Риски.** Именно здесь веб терял поля.
**Тесты.** Круговой: запись → форма → сохранение → та же запись, поле в поле.
**Checkpoint.** `migration: форма, маршрут и деньги`
**Сложность.** ОВ
```
Recommended: Sonnet
Escalate to Opus if: —
```

---

### Итерация 25 — Карточка: фазы, шапка, плитка дня

**Цель.** Заложить структуру самой крупной подсистемы (2 257 строк).
**Делаем.** Разбор структуры по `docs/11_EVENT_CARD.md` и `12_CARD_ARCHITECTURE`;
три вида по фазе (до / во время / после); шапка со знаком события; плитка дня;
студийный час; стопка карточек дня.
**Не делаем.** Блоков и маршрута — следующие итерации.
**Файлы.** `LightPlanUI/Screens/Card/*`.
**Зависит от.** 24
**Результат.** Каркас карточки стоит.
**Готово, когда.** Три фазы проверены на настоящих записях; граница «во время»
считается от первой точки до последней плюс `TAIL_MIN`.
**Риски.** Ошибка структуры здесь дороже всего: под неё пишутся две следующие
итерации.
**Тесты.** Snapshot трёх фаз × четыре группы жанров.
**Checkpoint.** `migration: карточка, фазы и шапка`
**Сложность.** В
```
Recommended: Opus для разбора структуры, Sonnet для сборки
Reason: состав и порядок — продуктовое решение с историей; представления
механические.
```

---

### Итерация 26 — Карточка: блоки и порядок

**Цель.** 14 блоков и перестановка.
**Делаем.** Все блоки; порядок по умолчанию; свой порядок группы жанров;
`AFTER_FIRST` для фазы «после» у заказа; режим перестановки драгом; свёртки;
свет как условие; погода; наложения; цепочка сделки; заказ.
**Не делаем.** Маршрут и референсы — следующая итерация.
**Файлы.** `LightPlanUI/Screens/Card/*`.
**Зависит от.** 25
**Результат.** Карточка наполнена.
**Готово, когда.** Порядок переживает перезапуск; «после» у заказа выносит
бумаги и деньги вперёд; свет появляется ровно по трём условиям (инвариант 9).
**Риски.** Порядок блоков хранится по группе, а не по съёмке — легко перепутать.
**Тесты.** Перестановка и её сохранение; появление света на границах.
**Checkpoint.** `migration: карточка, блоки и порядок`
**Сложность.** ОВ
```
Recommended: Sonnet
Escalate to Opus if: —
```

---

### Итерация 27 — Карточка: маршрут, референсы, полный экран

**Цель.** Закрыть карточку.
**Делаем.** Лента точек маршрута со знаками и словом; состояние точки; «что
дальше» во время события; референсы съёмки и набор жанра вместе; полный экран
референсов; документы.
**Не делаем.** Мудборды целиком — отдельная итерация.
**Файлы.** `LightPlanUI/Screens/Card/*`.
**Зависит от.** 26
**Результат.** Карточка готова.
**Готово, когда.** Лента совпадает с вебом на свадебном дне из 12 точек;
полный экран листается и возвращается на ту же точку.
**Риски.** Знаки точек подбираются по слову (`POINT_WORDS`) — правило должно
переехать целиком, включая порядок проверок.
**Тесты.** Подбор знака по 40 словам; snapshot ленты.
**Checkpoint.** `migration: карточка, маршрут и референсы`
**Сложность.** В
```
Recommended: Sonnet
Escalate to Opus if: —
```

---

### Итерация 28 — Мудборды, организации, опросник

**Цель.** Три подсистемы, которые в первой редакции плана были почти не названы
(мудборды одни — 908 строк).
**Делаем.** Мудборды: галерея двух родов папок, чипы разделов, «переложить» без
копий; организации: карточка, съёмки организации, документы, архив
взаимодействия; опросник: ссылка, QR, отправка, разбор ответа и сверка с тем,
что уже записано.
**Не делаем.** Не заводим новых сущностей: мудборд — это `tag` на референсе.
**Файлы.** `LightPlanUI/Screens/Storage/*`, `Org/*`, `Quest/*`.
**Зависит от.** 27
**Результат.** Хранилище и заказчики работают.
**Готово, когда.** Перенос материала между съёмкой и подборкой жанра не делает
копий; ответ клиента накладывается на запись с показом расхождений.
**Риски.** Опросник — отдельная страница (`quest/wedding.html`); решить, живёт
она дальше на вебе или становится нативным экраном по ссылке.
**Тесты.** Перенос материала; разбор кода опросника.
**Checkpoint.** `migration: мудборды, организации, опросник`
**Сложность.** В
```
Recommended: Sonnet
Escalate to Opus if:
- судьба страницы опросника оказывается продуктовым вопросом.
```

---

### Итерация 29 — Жесты, движение, отдача, звук

**Цель.** Довести взаимодействие до нативного качества.
**Делаем.** Свайп по строке, стопка карточек, драг блоков, щипок карты,
переходы между днями с направлением, отдача на касаниях **и на детентах**,
звуки щелчка, затвора и срыва, знакомство поверх живого интерфейса.
**Не делаем.** Новых жестов не добавляем.
**Файлы.** по всем экранам.
**Зависит от.** 28
**Результат.** Приложение отзывается как прибор.
**Готово, когда.** Ни один жест не конфликтует с системным (свайп назад,
скролл); 60 кадров на всех переходах; отдача гасится системными настройками.
**Риски.** Конфликт `DragGesture` со скроллом — классическая беда SwiftUI.
**Тесты.** Сценарный лист вручную; замер кадров.
**Checkpoint.** `migration: жесты, отдача и звук`
**Сложность.** В
```
Recommended: Sonnet
Escalate to Opus if:
- конфликт жестов не решается за два захода.
```

---

### Итерация 30 — Уведомления и календарь

**Цель.** То, ради чего уходили с веба.
**Делаем.** `UNUserNotificationCenter`: **студийный час**, вечерний брифинг,
напоминание о выезде, испортившийся прогноз к съёмке. Двусторонняя связь с
календарём вместо разовой выгрузки. «Поделиться» и работа с файлами.

**Что уже сделано в вебе (8 сентября 2026, DECISIONS, «Прогноз — переменная
величина» и «Студийный час стал таймером, а не строкой»).** Испортившийся
прогноз, появившееся наложение и **студийный час** считаются и говорятся —
баннером при открытии приложения и при возврате в него, плюс строкой в
карточке и меткой в списке. Готовы модель порога (смена категории неба либо
20 пунктов балла заката), три памяти `wxSeen`/`wxTold`/`studioTold`
отдельными ключами настроек, очередь баннеров с потолком в два и раздел
настроек с четырьмя тумблерами.

**Студийный час посчитан целиком, и числа менять не придётся.** Обе ступени
привязаны к дедлайну выхода (`rw.to − (60 − hourMin)`) и стоят за 10 и за 5
минут до него — это ровно `конец − 15` и `конец − 10` при часе в 55 минут,
то есть те самые два `UNCalendarNotificationTrigger`, что расписаны выше.
`studioTold = { дедлайн, ступень }` уже умеет переписываться при
подтверждённом продлении: дедлайн уехал — отметка перестала совпадать сама.
В карточке стоит кольцевой таймер на всю аренду — форма выбрана с оглядкой
на Dynamic Island и Live Activity, куда она и переезжает.

В натив переезжает не замысел, а расчёт: сюда остаётся принести расписание —
разбудить себя из свёрнутого состояния веб не умеет, и в этом вся итерация.
Три прежних пустышки («Вечерний брифинг», «Утреннее уточнение», «Напоминание
о выезде») с экрана сняты, ключи словаря оставлены — они вернутся здесь.

**Студийный час — первый в списке, и он же довод за весь переезд.** Условие
(DECISIONS, «Связь со студией опознаётся по совпадению номеров», уточнение
7 сентября 2026): час студии — 55 минут независимо от сервера; на двухчасовой
аренде приложение сигналит через 1 час 45 минут и повторяет через пять, чтобы
успеть выйти из зала. Считать это веб умеет и считает верно, а сказать — нет:
предупреждение живёт строкой в открытой карточке. Свёрнутая PWA на iOS не
исполняет `setTimeout`, `Notification Triggers` там нет вовсе, а Web Push
требует сервера — то есть ровно того, без чего напоминалка обязана работать.
Значит два локальных уведомления по расписанию (`UNCalendarNotificationTrigger`
на `конец − 15` и `конец − 10`), заводятся на каждую ячейку дня со студией,
переписываются при правке часов и при подтверждённом продлении аренды,
снимаются вместе с точкой.
**Не делаем.** Live Activity, Watch и виджеты — это redesign (итерация 36).
**Механизм календаря расписан заранее:** `docs/14_CALENDAR_MIRROR.md` — свой
`EKCalendar` внутри выбранного человеком источника (iCloud или его
Google-аккаунт), одностороннее зеркало на «только запись», обратная связь
отдельным шагом. Начинать не с чистого листа и не с `syncTarget`: нынешний
переключатель off/apple/google в натив не переезжает.
**Файлы.** `LightPlanData/Notify/*`, `Calendar/*`.
**Зависит от.** 27
**Результат.** Обещания настроек становятся правдой.
**Готово, когда.** Уведомление приходит при закрытом приложении; студийный час
зовёт дважды и на устройстве без сети; отказ в разрешении ничего не ломает;
уведомляем **только о том, чего человек не может увидеть сам**
(`04_ANTI_GOALS`).
**Риски.** Соблазн уведомлять о большем — запрещён правилом.
**Тесты.** Расписание — юнит-тесты; проверка на устройстве.
**Checkpoint.** `migration: уведомления и календарь`
**Сложность.** С
```
Recommended: Sonnet
Escalate to Opus if: —
```

---

### Итерация 31 — Облако и документы

**Цель.** Вернуть облако в новой роли.
**Делаем.** `CloudProvider` и три реализации, PKCE через
`ASWebAuthenticationSession`, папка приложения, загрузка файлов, ссылки на
просмотр, **обратная загрузка** (путь из облака в приложение — сегодня его нет),
полный архив с вложениями.
**Не делаем.** Облако не становится главным хранилищем — эта роль ушла к
файловой системе.
**Файлы.** `LightPlanData/Cloud/*`.
**Зависит от.** 12, 30
**Результат.** Копия, документы и референсы работают.
**Готово, когда.** Все три поставщика проходят круг: подключить → выгрузить →
забрать → отключить; поставщик без ключа честно показан отключённым.
**Риски.** OAuth в нативном — не редирект в вебе: своя схема возврата.
**Тесты.** Круговой на каждом поставщике; «токен отозван».
**Checkpoint.** `migration: облако фотографа`
**Сложность.** В
```
Recommended: Sonnet
Escalate to Opus if:
- роль облака расходится с записанным в ROADMAP.
```

---

### Итерация 32 — Аудит паритета

**Цель.** Не «сверить всё в конце» — это делалось по дороге, — а разобрать
накопленные расхождения и вынести приговор по каждому.
**Делаем.** Свод расхождений из всех экранных итераций; приговор по каждому:
ошибка переноса / допустимое улучшение / требует решения Алексея. Проверка всех
21 инвариантов подряд.
**Не делаем.** Не улучшаем по дороге.
**Файлы.** `docs/PARITY_REPORT.md`.
**Зависит от.** 31
**Результат.** Известно, где мы не Light Plan.
**Готово, когда.** Ни одно расхождение не осталось без приговора.
**Риски.** Усталость: на этом шаге хочется всё простить.
**Тесты.** Полный snapshot-набор; проход по инвариантам.
**Checkpoint.** `migration: аудит паритета`
**Сложность.** В
```
Recommended: Opus
Reason: суждение о том, что потеря, а что улучшение — не механическая работа.
```

---

### Итерация 33 — Тесты

**Цель.** Догнать покрытие там, где его не хватило.
**Делаем.** Недостающие юнит-тесты домена; полный snapshot-набор; UI-тесты на
пять сценариев: посмотреть свет на дату, завести съёмку, открыть карточку,
удалить и вернуть, восстановить из копии.
**Не делаем.** Не гонимся за процентом.
**Файлы.** `*Tests/*`.
**Зависит от.** 32
**Результат.** Регрессии ловятся сборкой, а не глазом.
**Готово, когда.** Весь набор зелёный на чистой машине.
**Риски.** Хрупкие snapshot-тесты — лечится допусками и фиксированной датой.
**Тесты.** Сами тесты.
**Checkpoint.** `migration: тесты`
**Сложность.** С
```
Recommended: Sonnet
Escalate to Opus if: —
```

---

### Итерация 34 — Выпуск

**Цель.** Приложение попадает на телефон Алексея не через Xcode.
**Делаем.** Подпись, профили, App Store Connect, TestFlight, страница
конфиденциальности (данные не покидают устройство — это надо уметь сказать),
описание, снимки экранов.
**Не делаем.** Публикацию в App Store решает Алексей отдельно.
**Файлы.** конфигурация проекта, `docs/RELEASE.md`.
**Зависит от.** 33
**Результат.** Бета в TestFlight заменяет `/beta/` на Pages.
**Готово, когда.** Сборка ставится с TestFlight и обновляется.
**Риски.** Требования к приватности при работе с геолокацией и облаком.
**Тесты.** Установка на чистое устройство.
**Checkpoint.** `migration: выпуск и TestFlight`
**Сложность.** С
```
Recommended: Sonnet
Escalate to Opus if: —
```

---

### Итерация 35 — Финальный архитектурный аудит

**Цель.** Убедиться, что получилось SwiftUI-приложение, а не порт HTML.
**Делаем.** Проверка границ модулей; поиск протёкшей веб-логики (ручные
измерения вместо layout, императивные перерисовки, строковые ключи в
сравнениях, обходы из корзины 2, которые всё-таки переехали); список долга.
**Не делаем.** Не переписываем — фиксируем.
**Файлы.** `docs/14_NATIVE_AUDIT.md`.
**Зависит от.** 34
**Результат.** Честная картина.
**Готово, когда.** По каждому пункту решение: чиним сейчас, чиним потом,
принимаем.
**Риски.** —
**Тесты.** —
**Checkpoint.** `migration: архитектурный аудит`
**Сложность.** В
```
Recommended: Opus
Reason: финальный архитектурный аудит.
```

---

### Итерация 36 — Native redesign (только после паритета)

**Цель.** То, чего веб не мог.
**Делаем.** Разбираем корзину 3: Dynamic Island с отсчётом золотого часа, Apple
Watch, движение состояний света, виджеты. (Стекло отсюда ушло: оно
системное с первой экранной итерации, решение 21 сентября 2026.)
**Не делаем.** Ничего из этого до закрытия итерации 32.
**Зависит от.** 35
**Checkpoint.** отдельные, вне серии `migration:`
```
Recommended: Opus для отбора, Sonnet для реализации
Reason: отбор — продуктовое решение против инвариантов.
```

---

## Potential Native Improvements

Корзина 3. Сюда складывается всё, что заметно по дороге, но **не делается во
время миграции**. Каждый пункт ждёт отдельного решения после итерации 32.

| # | Предложение | Откуда | Проверять против инварианта |
| --- | --- | --- | --- |
| 1 | Тень светила как свойство слоя вместо нарисованного круга | ROADMAP | 6, 7 |
| 2 | ~~Умное стекло (`Material`, `.glassEffect`) под куполом, в листе, в таббаре~~ — **вынесено из корзины 21 сентября 2026**: стекло сразу системное в экранных итерациях, имитацию веба не переносим (DECISIONS, «Стекло в нативе — системное») | ROADMAP | 21 |
| 3 | ~~Отдача на детентах ползунка и барабана~~ — **не корзина** (21 сентября 2026): делается итерациями 17 и 29, как и написано в § 4.11 | код 17240 | 4 |
| 4 | ~~Разные тики отдачи и отдача на событие~~ — **не корзина** (21 сентября 2026): итерация 29, § 4.11 | код 17240 | 15 |
| 5 | Кегль заголовка через `minimumScaleFactor` | код 14686 | — |
| 6 | Локальная копия как полный архив с вложениями | ROADMAP | 19 |
| 7 | Обратная загрузка из облака в приложение | ROADMAP | 19 |
| 8 | Двусторонняя связь с календарём | ROADMAP | 13 |
| 9 | Dynamic Island: живой отсчёт золотого часа | ROADMAP | 12, 15 |
| 10 | Apple Watch: осложнение с кольцом отсчёта | ROADMAP | 12 |
| 11 | Плавное движение состояний света и поворот звёздного поля | ROADMAP | 6 |
| 12 | `SolarDay` как значение делает расчёт нескольких дней дешёвым — недельная лента может стать точнее | § 5.1 | 5 |
| 13 | macOS как полноценный второй экран, а не порт | § 4.1 | 21 |
| 14 | Опросник нативным экраном вместо отдельной веб-страницы | итерация 28 | 19 |
| 15 | В день перевода часов события после перевода стоят на час мимо стены: модель берёт одно смещение на сутки (из кода, не мерено на вебе) | итерация 7 | — |
| 16 | Луна за полярным кругом: когда она над горизонтом дольше окна поиска (двое с половиной суток), веб не находит дуги и говорит «не всходит», хотя луна висит в небе. Замер 9а: на полюсе так 365 суток из 365; на 78° «не всходит» 245 из 365, сколько из них луна на деле над горизонтом — не мерено. У солнца такой случай уже решён: «не заходит» | итерация 9а | — |

---

## Definition of Done

Итерация закрыта, когда выполнено **всё**, а не первое.

1. **Компиляция** — оба таргета, без предупреждений в своём коде.
2. **Запуск** — симулятор и живой телефон.
3. **Поведение** — сценарий итерации проходится руками.
4. **Краевые случаи** — названные в итерации проверены отдельно.
5. **Числа** — паритет зелёный; расхождения объяснены, а не списаны. У проекта
   есть правило «мерить числами до коммита» — оно действует и здесь.
6. **Снимок** — для экранной итерации: сверка с `node tools/shot.js` или с
   телефоном; отличия записаны в `docs/PARITY_REPORT.md` в тот же день.
7. **UX** — ни один из 21 инварианта не нарушен; нарушен намеренно — разбор в
   `DECISIONS.md`.
8. **Корзины** — ни один пункт корзины 2 не переехал дословно; ни один пункт
   корзины 3 не сделан заранее.
9. **Тесты** — названные в итерации написаны и зелёные.
10. **Регрессии** — весь прежний набор зелёный.
11. **Запись** — что решено и почему, в `DECISIONS.md`; что осталось — в
    `ROADMAP.md`.

`Build succeeded` не является ни одним из этих пунктов.

---

## Git strategy

- Веб живёт в `main`, нативное — в `native`. Слияния между ними нет. Общее —
  `docs/`, `DECISIONS.md`, `ROADMAP.md`: правятся в обеих ветках.
- Один checkpoint — одна итерация. Крупные разбиты заранее (карточка на три,
  форма на две, планировщик на две) — именно поэтому, а не по объёму текста.
- Сообщения по-русски, о смысле, как принято в проекте:
  ```
  migration: солнечный движок
  migration: прототип таймбара
  migration: хранилище и импорт из PWA
  ```
- В одном checkpoint **не смешиваются** архитектурный рефакторинг, новый UI и
  починка постороннего. Нашлась посторонняя ошибка — отдельный коммит (правило
  «не чинить заодно»: правка сверх жалобы стирает показания прибора).
- Каждый checkpoint оставляет проект собираемым и запускающимся.
- Прототипы (`Spikes/`) коммитятся отдельно и в приложение не тянутся.

---

## Итоговая таблица

`∥` — итерация вне критического пути, может идти параллельно.

| # | Iteration | Complexity | Depends on | Model | Opus needed? |
| --- | --- | --- | --- | --- | --- |
| 0 | Reverse engineering ✔ | В | — | Opus | Да |
| 1 | Архитектура вчерне, политика веба и выпуска ✔ черновик | В | 0 | **Opus** | **Да** |
| 2 | Скелет проекта | Н | 1 | Sonnet | Нет |
| 3 | **Spike: таймбар** | ОВ | 2 | **Opus** | **Да** |
| 4 | **Spike: холст карты** | В | 2 | **Opus** | **Да** |
| 5 | Архитектура закрыта | С | 3, 4 | **Opus** | **Да** |
| 6 | Стенд паритета | С | 5 | Sonnet | Нет |
| 7 | Solar Engine | С | 6 | Sonnet | Только при расхождении |
| 8 | Состояние света и палитра | Н | 7 | Sonnet | Нет |
| 9 | Луна, Млечный Путь, затмения ∥ | С | 7 | Sonnet | Нет |
| 9а | Окна неба: луна над горизонтом, окно Млечного Пути ∥ | С | 7, 9 | Sonnet | Нет |
| 10 | Погода и закатный балл ∥ | С | 7, 8, 9а | Sonnet | Нет |
| 11 | Домен: записи, жанры, правила | В | 8 | Sonnet | При споре с docs |
| 12 | **Хранилище и импорт живых данных** | В | 11 | Sonnet | При неукладываемых данных |
| 13 | Локация ∥ | С | 11 | Sonnet | Нет |
| 14 | Словарь и даты ∥ | С | 2 | Sonnet | Нет |
| 15 | Знаки ∥ | Н | 2 | Sonnet | Нет |
| 16 | Временная модель | В | 7 | Sonnet | При расхождении с DECISIONS |
| 17 | Таймбар в приложении | В | 3, 10, 15, 16 | Sonnet | Нет |
| 18 | Купол | В | 8, 9, 15, 17 | Sonnet | При цветовых расхождениях |
| 19 | Экран «Свет» | С | 10, 18 | Sonnet | Нет |
| 19а | Экран «Настройки»: каркас и свои разделы ∥ | С | 13, 14, 19 | Sonnet | Нет |
| 20 | Экран «Карта» | ОВ | 4, 9, 13, 19 | Sonnet | Нет |
| 21 | Планировщик: месяц, неделя, день | ОВ | 12, 19 | Sonnet | При дублировании состояния |
| 22 | Планировщик: год, итоги, корзина | В | 21 | Sonnet | Нет |
| 23 | Форма: состав, барабаны, клиент | В | 21 | Sonnet | При споре с матрицей полей |
| 24 | Форма: маршрут, деньги, документы | ОВ | 23 | Sonnet | Нет |
| 25 | Карточка: фазы, шапка, плитка дня | В | 24 | **Opus** → Sonnet | **Да** (структура) |
| 26 | Карточка: блоки и порядок | ОВ | 25 | Sonnet | Нет |
| 27 | Карточка: маршрут и референсы | В | 26 | Sonnet | Нет |
| 28 | Мудборды, организации, опросник | В | 27 | Sonnet | Нет |
| 29 | Жесты, движение, отдача, звук | В | 28 | Sonnet | При конфликте жестов |
| 30 | Уведомления и календарь | С | 27 | Sonnet | Нет |
| 31 | Облако и документы | В | 12, 30 | Sonnet | Нет |
| 32 | **Аудит паритета** | В | 31 | **Opus** | **Да** |
| 33 | Тесты | С | 32 | Sonnet | Нет |
| 34 | Выпуск и TestFlight | С | 33 | Sonnet | Нет |
| 35 | **Финальный аудит** | В | 34 | **Opus** | **Да** |
| 36 | Native redesign | — | 35 | Opus → Sonnet | Да (отбор) |

**Критический путь:** 0 → 1 → 2 → 3 → 5 → 6 → 7 → 8 → 11 → 12 → 16 → 17 → 18 →
19 → 21 → 23 → 24 → 25 → 26 → 27 → 32.

**Вне пути и потому параллелятся:** 9, 9а, 10, 13, 14, 15 — Sonnet может вести их
одновременно с основной линией. Итерация 4 (холст карты) тоже параллельна: она
блокирует только экран «Карта» (20).

---

## Recommended Workflow

### Критерий выбора модели

Первая редакция называла триггером Opus «требует анализа большого количества
существующего кода». В этом проекте это неверно и уводит Opus не туда: большие
задачи здесь как раз механические, потому что спецификация написана; а
неоднозначные — маленькие. Карточка события — 2 257 строк работы исполнителя.
Взвод и срыв — 166 строк, где нужен архитектор.

Настоящий критерий — **неоднозначность и цена ошибки**:

| Признак | Модель |
| --- | --- |
| Есть эталон, фикстуры и критерий числом | Sonnet |
| Спецификация написана, работы много | Sonnet |
| Ошибка компиляции, механический перенос, тесты по описанию | Sonnet |
| Решение может отменить архитектуру | Opus |
| Два несравнимых пути и внешние ограничения | Opus |
| Ошибка обнаружится через месяц и будет стоить переделки | Opus |
| Под вопросом инвариант Core UX | Opus |
| Код расходится с `docs/` | Opus |

### Порядок

**Opus в начале — итерации 1, 3, 4, 5.** Это четыре итерации подряд, и они
идут первыми не потому, что «сначала архитектура», а потому что только они
могут отменить всё остальное. Прототипы стоят третьим и четвёртым, а не в
середине: и таймбар, и холст карты проверяются **на выдуманных числах**, им не
нужны ни солнце, ни данные. Узнать, что холст не получается, надо на первой
неделе, а не когда под него написаны десять экранов.

**Дальше Sonnet, долго — итерации 6…24.** Девятнадцать итераций подряд, где
задача каждый раз хорошо определена: эталон в вебе, фикстуры на диске, критерий
готовности числом. Opus здесь не нужен и будет дорогой ошибкой. Внутри этой
полосы пять итераций (9, 10, 13, 14, 15) идут параллельно.

**Opus один раз в середине — итерация 25, только на разбор структуры карточки.**
Дальше её собирает Sonnet тремя заходами.

**Sonnet — итерации 26…31.**

**Opus в конце — 32 и 35.** Паритет и аудит: суждение, а не код.

### Чего Opus делать не должен

- писать SwiftUI-представления по готовой спецификации;
- переносить таблицы (жанры, сроки, папки, знаки, словарь);
- чинить ошибки компиляции;
- писать тесты по описанным критериям;
- переводить формулы, для которых есть фикстуры.

### Когда переключаться на Opus вне плана

- Sonnet дважды не свёл расхождение с эталоном;
- задача упёрлась в выбор между двумя архитектурами;
- найдено противоречие между кодом и `docs/`;
- решение затрагивает больше двух подсистем сразу;
- под вопросом один из 21 инварианта Core UX;
- сработало правило остановки (§ 7).

### Правила сессии

Действуют `CLAUDE.md` и память проекта: ответы по-русски; вывод сборок
фильтруется; `.pbxproj` целиком не читается; длинные логи уходят на диск и
грепаются; крупные разборы — через субагента с моделью по задаче; `/clear`
между несвязанными итерациями, `/compact` внутри длинной; перед перерывом с
открытой задачей — `/compact`, с закрытой — запись итога в память и `/clear`.

---

## Главное правило

Сначала понять систему. Потом проверить прототипом то, что может отменить
понимание. Потом спроектировать. Потом переносить маленькими работающими
шагами. И только после паритета — улучшать.

Нативный Light Plan состоялся, когда фотограф, открыв его, не заметил, что
приложение сменило технологию, — и через минуту заметил, что оно стало быстрее,
тише и умеет то, чего веб не мог.
