name = "light-plan-og-reader"
main = "worker.js"
compatibility_date = "2024-09-01"

# Ни KV, ни D1, ни секретов — читалка ничего не хранит и не запоминает.
# Разворачивается на бесплатном подадресе *.workers.dev, свой домен не нужен.
