# Промпт: хвосты, заход Г

Заготовка сообщения для новой сессии Claude Code. Собрана 16 сентября 2026,
после заходов А, А2, Б, Б2 и В: их списки «Найдено рядом, не чинилось»
сведены в один и перемерены по коду. Постановка живёт в ROADMAP, раздел
«Хвосты заходов А–В — сведены 16 сентября 2026», — промпт на неё ссылается,
а не пересказывает.

Перед отправкой стоит перечитать список в ROADMAP: между этой записью и
заходом Г могут пройти волны, и часть пунктов закроется сама, как закрылись
пять прежних.

**Что решить до отправки (иначе сессия придёт с этими вопросами):**
- `cdMoney` — резать или оставить;
- как звучит «Съёмка ждала: {wish}» безлично (для `light.waitedSunset` слова
  уже дал: «Ожидался закат, …»).

Ответы можно дописать прямо в сообщение — тогда сессия не будет
останавливаться.

---

## Сообщение

Хвосты, заход Г — уборка после заходов А–В

Ты работаешь над Light Plan — PWA-прототипом для фотографов. Основная папка —
`/Users/alexey/Documents/Light_Plan/Light_Plan`. Код беты: `beta/index.html`
(~35 тысяч строк), `beta/lang.js`. Корень `index.html` — живая версия, его не
трогать: правки только в бету, а `index.html` уезжает в корень побайтно.

Отвечай по-русски. Прочитай `CLAUDE.md` целиком: правила экономии контекста и
раздел «Parallel sessions» (пункты 18–20) обязательны.

Постановка — в `ROADMAP.md`, раздел «Хвосты заходов А–В — сведены 16 сентября
2026»: девять открытых пунктов, пять закрывшихся сами и список того, что не
наше. Образец такой уборки — заход В: итог в ROADMAP под «Заход В», разбор
«Хвосты, заход В» в конце `DECISIONS.md`, раздел «Старый вид карточки» в
`archive/ARCHIVE.md`. Прочитай их до работы.

### Шаг 0

- `git log --oneline -20`, `git worktree list`, `SESSIONS_CHAT.md` по полному
  пути. Если в `git worktree list` висит чужой worktree или в чате есть
  незакрытая запись о взятии беты — сперва прочитай, чем занят сосед, и
  реши, не столкнётесь ли.
- Worktree:
  `git -C /Users/alexey/Documents/Light_Plan/Light_Plan worktree add .claude/worktrees/tails-g -b wt/tails-g origin/main`,
  затем `EnterWorktree`. Напиши в конец `SESSIONS_CHAT.md` (подпись «Хвосты,
  заход Г»), что взял и где режешь.
- Превью: своя запись `wt-tails-g` в `launch.json` папки запуска, порт из
  8791–8799. Сперва проверь `lsof`. По закрытии запись убери. На
  `localhost:8788` не проверяй.
- **Каждый пункт — гипотеза.** Список сведён 16 сентября, и уже тогда пять
  пунктов из четырнадцати оказались закрытыми соседними волнами. Мерь
  заново, прежде чем чинить: записанный диагноз — это гипотеза, а не факт.

### Порядок

Пункты 1–4 трогают `beta/index.html`, 5–9 — тестовые данные и приборы. Если
беты одновременно касается кто-то ещё, начни с 5–9: они по коду беты не
ходят.

### Пункты

Смысл каждого — в ROADMAP, здесь только то, что важно для порядка работ.

1. **`cdMoney`** — решение Алексея. Если резать: вместе с прячущей строкой в
   `renderEventCard`, обработчиком разворота, CSS `.card-money` и разделом в
   `archive/ARCHIVE.md` — раздел «Старый вид карточки» там уже есть, допиши
   в него, а не заводи второй. Полный пересчёт объявлений без ссылок до и
   после, как в заходе В (самозапускающиеся функции исключить).
2. **`SORT_LABELS`** — перепроверь, в том числе строковые обращения, и удали
   строкой оглавления архива, как `CLOUD_NAMES`.
3. **«Съёмка ждала: {wish}»** (`card.wishMissed`) — формулировка Алексея.
   Правится русский; английский, испанский и китайский не трогать, как в
   заходе А2.
4. **`repSumHint`** — сперва замер: есть ли видимый разрыв, пока место в
   форме правят. Нет разрыва — пункт закрывается замером, а не правкой.
5. **`beta/seed.js`: `addr` → `address`** у обеих студий. Проверить, что
   адрес доехал до карточки, а не только до снимка.
6. **`me.met` в засевах** — все четыре страницы (`_seed.html`,
   `_seed_studio.html`, `_seed_wx.html`, `_seed_season.html`). Смотри, как
   это поле ставит `closeStartSheet`, и ставь так же.
7. **Сироты в `lp`** — запустить `beta/_lpsweep.html` и убедиться, что после
   уборки картинки корня целы. В `lp` вправе писать только корень: страница
   разовая, ссылки из приложения не заводить.
8. **«картинки N МБ»** — это не поломка, а цена честного счёта по каналам.
   Трогать, только если Алексей скажет, что число должно быть другим.
9. **`LANG_TABLE.md`** — перегенерировать (`node tools/langdoc.js`), сверить
   с `node tools/langcheck.js`.

### Шлюз и финал

- `node tools/langcheck.js`, `node tools/datecheck.js`,
  `node tools/mergecheck.js`, `node tools/icscheck.js`,
  `node tools/pointcheck.js`, `node --check` встроенных скриптов — выводить
  только несовпадения и итог.
- PWA на своём порту: карточка съёмки во всех трёх видах, встреча, «Данные и
  облако». Каждый тронутый засев — на своём порту, по отдельности: карточка
  открывается, маршрут, время и свет те же, что задуманы в заметке засева,
  ошибок JS 0. Засев убрать по закрытии.
- **Ловушка засевов:** часы точки маршрута в записи — число минут от полуночи
  первого дня, а не строка «ЧЧ:ММ». Строка — вид ячейки в форме
  (`cellMin`/`setCellMin`). Договор записан в шапке каждого засева, не
  «чини» его. Про засев в ярлык симулятора — DECISIONS, поиск по
  `_seed_studio.html`: «донести посев до webclip не вышло».
- Коммит в worktree → `git fetch origin && git rebase origin/main` →
  `git push origin HEAD:main` (без force) →
  `git -C /Users/alexey/Documents/Light_Plan/Light_Plan pull --ff-only`.
  Команды по одной. Worktree убрать, когда коммиты окажутся в `origin/main`.

### Итог

- в `ROADMAP.md` под «Хвосты заходов А–В» — «Итог <дата>»: коммиты, сколько
  строк и функций вырезано числами, что ушло в архив, что оказалось живым,
  что закрылось замером без правки, «Найдено рядом, не чинилось»;
- в конец `DECISIONS.md` — разбор разделом «Хвосты, заход Г»;
- в `SESSIONS_CHAT.md` — запись о закрытии.
