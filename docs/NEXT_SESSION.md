# Сейчас — состояние миграции для новой сессии

Хозяин — **координатор проекта «LightPlan» в Claude** (инструкции —
`docs/orchestrator_project_instructions.md`). Файл **переписывается, а не
дописывается**, до ~100 строк. Решения целиком — `DECISIONS.md`, задания —
`SWIFT_MIGRATION_PLAN.md` § 8, передача между живыми чатами —
`SESSIONS_CHAT.md`. Прежние версии — в git.

Обновлено: **26 сентября 2026, утро** — закрыты и отправлены 22 и 23; дальше 24.

## Сейчас

- **Закрыты 22 и 23** (приняты на телефоне, подробности — ROADMAP, итоги —
  план § 8). 22: лента года, «Год целиком», статистика, поиск, корзина,
  «Занять время», «Сдача материала». 23: форма записи — жанр, клиент / пара /
  заказчик, дата и время, заметки, организация, черновик; «Профиль».
  Натив `main` = `de98a9c` (чекпоинт 22; чекпоинт 23 — `eeb36f3`).
- **Экраны готовы:** «Свет», «Карта» (20а–20е), «Настройки» (каркас),
  «Съёмки» — месяц, неделя, день, год, итоги; форма без маршрута и денег.
- **На Mac остались** (удалить — только по слову Алексея): worktree 22 в
  нативе и в документах, ветка `wt/23` без папки, симуляторы «LP wt-22» и
  «LP wt-23». Диск Mac 25.09 был почти полон — чистка освободит место.
- **Чужое, не трогать:** в `Light_Plan` неотслеживаемые `beta/Знаки мест/` и
  два старых `docs/PROMPT_*.md`.

## Правила работы (Алексей)

- **Итерация — шагами (26.09).** До старта итерация делится на шаги меньше
  ~300 тыс. токенов; один шаг — один тред. Между шагами: итог в план и
  `SESSIONS_CHAT.md`, коммит `wip:` в ветку `wt/<nn>`. Тред, простоявший
  больше часа, новой работы не получает — новый тред.
- **Тяжёлое — по одному (26.09).** Код, сборки, `make shots` — один тред за
  раз; параллельно только лёгкое (документы, чтение, проверки). Иначе все
  ветки упираются в лимит, а после сброса сами продолжают и съедают окно.
- **Передача — только закоммиченной работы**, своя ветка и свой симулятор
  `LP <ветка>`; базовый iPhone 17 Pro Max не занимать; в `main` — на чекпоинте.
- **Экранная итерация закрывается** парами веб / натив (`make shots`) по
  числам, потом сборка на телефоне Алексея и его слово.
- **Стекло** — встроенное `.glassEffect` везде, где у веба имитация; вид
  прототипа, не системные меню (`check_glass.sh`).
- **Модели:** до выхода Sonnet 5.5 (~2.10) — Opus 5.5 на всех тредах. После —
  Opus для 24, 25, 29, 32, 35, 36, остальное Sonnet 5.5.
- **Веб заморожен** — эталон; ошибку веба записывать, не чинить.
- **Отправка в GitHub** публичного — только по слову Алексея.

## Решения Алексея, которые держать

- Барабаны часа и минуты в форме — **системное колесо iPhone** (26.09).
- Удаление занятого времени (выходной, дорога, перелёт) — полоса «Вернуть»
  на 6 с, в корзину не идёт; съёмка удаляется **долгим нажатием** (веер
  «Заполнить / Удалить»), свайпа нет — как в вебе (22).
- Пустые месяцы в статистике — **ровной колонкой**; у беты они сдвинуты
  случайно. Алексей может попросить «как в бете» — правка небольшая.
- Анимации переходов месяц ↔ неделя ↔ день — **итерация 29** (сделаны не все,
  Алексей заметил 26.09; вписано в задание 29).
- Режим маршрута на «Карте» — итерация 24. Украинского языка не будет.
- Доступность (VoiceOver у своих приборов) — с 22; «Свет», купол, таймбар,
  прибор «Карты» догоняют в 29.

## Дальше — итерация 24 «Форма: маршрут, деньги, сдача, документы»

Opus 5.5, ветка `wt/24`, симулятор «LP wt-24». Чекпоинт
`migration: форма, маршрут и деньги`. Главное условие: правка **меняет**
запись — поля вне формы (`fromMeetId`, `questSent`, `grewToId`) сохраняются
сами; круговой тест «запись → форма → сохранение → та же запись».
**Предложение шагов** (каждый — свой тред, по одному):

1. **Хвосты формы 23:** повторы (`rep-blk`), уточнения жанра (долгий тап по
   плитке), лист «Мои жанры», «Удалить» из формы. Пары формы; проверить на
   телефоне минуту света не по шагу (18:37 в колесе показано 18:35).
2. **Место и маршрут в форме:** блок «Место и адрес», точки маршрута с тремя
   входами (адрес / студия / координаты), пожелания к погоде и проверка
   «замысел против прогноза» (качество неба для подсказки времени).
3. **Деньги и сдача:** ставка, пакеты, расходы, предоплата, валюта; барабан
   срока сдачи и «сдан». Сверка сумм с вебом числами.
4. **Референсы, документы, оборудование; сохранение.** Круговой тест записи,
   пары всей формы, сборка на телефоне — слово Алексея по форме.
5. **Маршрут на «Карте»:** визир, набор точек, «Сделать съёмкой»; кнопка
   «Мои места» в шапке «Карты»; подпись «точка убрана / переехали»; переезд
   держит текущее приближение, а не 14. Пары, телефон, чекпоинт, отправка.

Если шаг 5 окажется большим — вынести его в отдельную итерацию 24а (решение
координатора, Алексею не мешает).

## Открытые нити

- **Хвосты 23 розданы:** повторы, уточнения жанра, «Мои жанры» — 24; вход
  «Встреча» — 28; «сначала копия» / «чей номер» при смене номера — 31.
- **Бета:** жанр «животные» в статистике пишется кодом `genreN.animals` (нет
  слова в `lang.js`). Натив пишет имя жанра; веб не трогали.
- **Строка загрузки под неделей** — ошибка эталона, в натив не переносится.
- **Без хозяина:** «Музыка» и «Завершать вручную» (настройки «Съёмок»),
  ссылка на Telegram, «Правовая информация». Музыка — вопрос Алексею
  (в 25 / отдельной после карточки / не в первый выпуск).
- **Кадр «Карты» на телефоне не мерен** — только если Алексей заметит
  торможение.
- **Экспорт с телефона** для 12 — нужен настоящий файл.
- Ветки `wt/sun-arc`, `wt/sun-portal` в `Light_Plan` — старые, влиты;
  удалить может хозяин.
- `ws:CLAUDE.md` — 132 строки при своей норме 125.
- **Расход (замеры):** 23 на Sonnet ~245 тыс.; 22 вторая сессия на Opus
  115 → 360 тыс. за 17 мин; третья со строгим заданием 60 → 170 тыс. —
  строгое задание работает.

## Ждёт Алексея

- слово на старт 24 (шаг 1) и согласие с делением на шаги;
- удаление worktree 22, ветки 23 и симуляторов LP wt-22/23;
- оплата Apple Developer Program (срок был 26.09) — после неё перемерить
  iCloud, Push, TestFlight (30, 31, 34);
- файл экспорта с телефона (12).
