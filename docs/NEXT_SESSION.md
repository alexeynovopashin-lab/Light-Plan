# Сейчас — состояние миграции для новой сессии

Хозяин — **координатор проекта «LightPlan» в Claude** (инструкции —
`docs/orchestrator_project_instructions.md`). Файл **переписывается, а не
дописывается**, до ~100 строк. Решения целиком — `DECISIONS.md`, задания —
`SWIFT_MIGRATION_PLAN.md` § 8, передача между живыми чатами —
`SESSIONS_CHAT.md`. Прежние версии — в git.

Обновлено: **26 сентября 2026, вечер** — итерация 24: шаг 1 принят Алексеем
на телефоне; дальше шаг 2.

## Сейчас

- **Закрыты 22 и 23** (подробности — ROADMAP, итоги — план § 8). Натив
  `main` = `de98a9c` (чекпоинт 22; чекпоинт 23 — `eeb36f3`).
- **Итерация 24 «Форма: маршрут, деньги, сдача, документы» — в работе, 5
  шагов.** Шаг 1 (хвосты формы 23) сделан и принят на телефоне: «всё
  отлично» — после правок удержания жанра и матовой плашки корзины. Натив
  `da2265a` (документы — `996590e`). Сделано: повторы (`rep-blk`),
  уточнения жанра, лист «Мои жанры», «Удалить» из формы, системное колесо
  часа/минуты (`UIPickerView`, минута от света видна без округления).
  Дальше — шаг 2, см. ниже.
- **Экраны готовы:** «Свет», «Карта» (20а–20е), «Настройки» (каркас),
  «Съёмки» — месяц, неделя, день, год, итоги; форма без маршрута и денег.
- **Уборка на Mac сделана 26.09** — worktree и симуляторы итераций 22/23
  удалены, свободно 14 ГБ.
- **Чужое, не трогать:** в `Light_Plan` неотслеживаемые `beta/Знаки мест/`
  и два старых `docs/PROMPT_*.md`.

## Правила работы (Алексей)

- **Итерация — шагами (26.09).** До старта итерация делится на шаги меньше
  ~300 тыс. токенов; один шаг — один тред. Между шагами: итог в план и
  `SESSIONS_CHAT.md`, коммит `wip:` в ветку `wt/<nn>`. Тред, простоявший
  больше часа, новой работы не получает — новый тред.
- **Тяжёлое — по одному (26.09).** Код, сборки, `make shots` — один тред за
  раз; параллельно только лёгкое (документы, чтение, проверки). Иначе все
  ветки упираются в лимит, а после сброса сами продолжают и съедают окно.
- **Передача — только закоммиченной работы**, своя ветка и свой симулятор
  `LP <ветка>`; базовый iPhone 17 Pro Max не занимать; в `main` — на чекпоинте.
- **Экранная итерация закрывается** парами веб / натив (`make shots`), потом
  сборка на телефоне и слово Алексея. Стекло — встроенное `.glassEffect`
  везде, где у веба имитация, вид прототипа, не системные меню (`check_glass.sh`).
- **Модели:** до выхода Sonnet 5.5 (~2.10) — Opus 5.5 на всех тредах. После —
  Opus для 24, 25, 29, 32, 35, 36, остальное Sonnet 5.5.
- **Веб заморожен** — эталон; ошибку веба записывать, не чинить.
- **Отправка в GitHub** публичного — только по слову Алексея.

## Решения Алексея, которые держать

- Барабаны часа и минуты в форме — **системное колесо iPhone**; в форме —
  два столбца `UIPickerView`, минута от света видна без округления до шага
  (шаг 1 итерации 24).
- Удержание на жанре работает **как двойной тап** — выбирает жанр и
  раскрывает уточнения, панель остаётся открытой, отдача при раскрытии;
  расходится с вебом по слову Алексея (шаг 1 итерации 24).
- Плашка «в корзине» — **матовая**, тон `--overlay-3` поверх стекла (шаг 1
  итерации 24).
- Удаление занятого времени — полоса «Вернуть» 6 с; съёмка — долгое
  нажатие, без свайпа; пустые месяцы статистики ровной колонкой; анимации
  переходов месяц↔неделя↔день → итерация 29.
- Режим маршрута на «Карте» — итерация 24. Украинского языка не будет.
- Доступность (VoiceOver у своих приборов) — с 22; «Свет», купол, таймбар,
  прибор «Карты» догоняют в 29.

## Дальше — итерация 24, шаг 2 из 5: «Место и маршрут в форме»

Opus 5.5, ветка `wt/24` (продолжение), симулятор «LP wt-24». Блок «Место и
адрес», точки маршрута с тремя входами (адрес / студия / координаты),
пожелания к погоде и проверка «замысел против прогноза» (качество неба для
подсказки времени).

**Шаги 3–5 после:** 3) деньги и сдача — ставка, пакеты, расходы,
предоплата, валюта, барабан срока сдачи и «сдан», сверка сумм с вебом
числами; 4) референсы, документы, оборудование, сохранение — круговой тест
записи, пары всей формы, сборка на телефоне и слово Алексея по форме;
5) маршрут на «Карте» — визир, набор точек, «Сделать съёмкой», кнопка «Мои
места» в шапке; переезд держит текущее приближение. Если шаг 5 большой —
в отдельную итерацию 24а.

## Открытые нити

- **Бета:** жанр «животные» в статистике пишется кодом `genreN.animals`
  (нет слова в `lang.js`). Натив пишет имя жанра; веб не трогали.
- **Строка загрузки под неделей** — ошибка эталона, в натив не переносится.
- **Без хозяина:** «Музыка» и «Завершать вручную» (настройки «Съёмок»),
  ссылка на Telegram, «Правовая информация». Музыка — вопрос Алексею.
- **Кадр «Карты» на телефоне не мерен** — только если Алексей заметит
  торможение. Экспорт с телефона для 12 — нужен настоящий файл.
- Ветки `wt/sun-arc`, `wt/sun-portal` в `Light_Plan` — старые, влиты,
  удалить может хозяин. `ws:CLAUDE.md` — 132 строки при норме 125.
- **Расход (замеры):** 23 на Sonnet ~245 тыс.; 22 вторая сессия на Opus
  115 → 360 тыс. за 17 мин; третья со строгим заданием 60 → 170 тыс.

## Ждёт Алексея

- слово на старт шага 2 итерации 24 (Opus, тред на Mac);
- оплата Apple Developer Program (срок был 26.09) — после неё перемерить
  iCloud, Push, TestFlight (30, 31, 34);
- файл экспорта с телефона (12).
