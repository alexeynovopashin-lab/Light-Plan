# Сейчас — состояние миграции для новой сессии

Хозяин — **координатор проекта «LightPlan» в Claude** (с 25 сентября 2026;
до того — чат «Дорожная карта» в Claude Code, закрыт). Его инструкции —
`docs/orchestrator_project_instructions.md`. Переписывает этот файл после
каждого отчёта итерации. Файл **переписывается, а не дописывается**; держать
до ~100 строк. Решения целиком — `DECISIONS.md`, задания итераций —
`SWIFT_MIGRATION_PLAN.md` § 8, передача между живыми чатами —
`SESSIONS_CHAT.md`. Здесь — то, что нужно знать до начала работы, и то, что
решено в чате дорожной карты. Прежние версии — в git.

Обновлено: **25 сентября 2026, вечер** — закрыты 19в, 21б, 21в; дальше 22 ∥ 23.

## Сейчас

- **Закрыты 24–25.09** (все приняты на телефоне, подробности — ROADMAP):
  20б и 21 (карта целиком, «Съёмки» месяц-неделя-день), 20в–20е (кнопки,
  булавки, стекло всей «Карты», тап по булавке), 21а (свой симулятор у
  ветки, `make rotor`), 13а (Крым по региону телефона). Основные папки
  чистые; чужие неотслеживаемые `beta/Знаки мест/`, `docs/PROMPT_*.md`.
- **19в, 21б, 21в закрыты 25.09**, приняты на телефоне: солнце пальцем по
  куполу и лист «Когда смотрим» (19в); щипок на OpenStreetMap и «голый
  холст» (21б, прибор `make pinch`); лист «Где снимаем» и «Мои места» из
  настроек (21в). Натив `main` = `5baa32c` (после — правка `make rotor`,
  21а: значок батареи не пустота). Все worktree убраны.
- **Инвентарь веба** — `docs/native_web_inventory.md` (≈290 функций с
  хозяевами). Музыка — вопрос Алексею (варианты: в 25 / отдельная после
  карточки / не в первый выпуск).
- **Потом 22 ∥ 23** (Sonnet), каждая на своём симуляторе.
- Лимит (25.09, 05:27 UTC, замер `get_usage`, план Pro): неделя 7 %, сброс
  29.09 18:00 UTC; пятичасовое окно 50 %, сброс 09:20 UTC. Узкое место —
  пятичасовое окно при 3–4 параллельных чатах, не неделя. Прежние «57 %
  недели» (24.09) — до сброса или другой шкалы, не сравнивать.

## Решено в чате дорожной карты — касается ближайших итераций

- **Размер сессии:** у ~300 тыс. токенов — промежуточный итог в план и
  `SESSIONS_CHAT.md`, свежий чат. Повторно не сжимать. 20б и 21 норму
  превысили — держать её.
- **Передача — только закоммиченной работы:** итерация живёт в своём
  worktree и ветке `wt/<задача>`, перед передачей — коммит `wip:` в ветку,
  в `main` — на чекпоинте.
- **Пары — каждая ветка на своём симуляторе** (с 21а, создаётся сам);
  базовый iPhone 17 Pro Max не занимать.
- **Экранная итерация закрывается парой снимков** веб / натив (`make shots`),
  сверенной числами, и места, и цвета. «Не проверено» вместо снимка не
  принимается (§ 5.4).
- **«Стекло в нативе — системное»** = вид прототипа веба на встроенном
  стекле (`.glassEffect`), **не** системные компоненты и меню. 21 сначала
  сделала системные меню и переделала. **Везде, где у веба имитация** —
  встроенное стекло (Алексей, 25.09): таблица 26 мест с хозяевами — план
  § 5.4; на «Карте» и «Съёмках» подделки нет (20д, `check_glass.sh`).
- **Режим маршрута на «Карте»** (центр компаса → булавка, отдача при
  построении) — итерация 24.
- **Доступность (пункт 12, с 22-й):** своим приборам — подпись и значение
  для VoiceOver. Догоняют: планировщик 21 — итерация 22; «Свет», купол,
  таймбар, прибор «Карты» — 29.
- **Модели (Алексей, 25.09):** до выхода Sonnet 5.5 (~2.10) — **Opus 5.5 на
  всех итерациях**, осознанная переплата: «Sonet допускает много ошибок».
  После — вернуться к плану: Opus для 24, 25, 29, 32, 35, 36.
- **Веб заморожен** — эталон. `beta/` не править; `tools/shot.js` учить
  можно. Ошибку веба, найденную сверкой, записывать, а не чинить молча.
- **Непринципиальное** решает исполнитель и пишет в DECISIONS «решение
  исполнителя»; **продуктовое** — Алексею, 2–3 варианта с выбором.
- **Запасная столица** без города и геолокации — страна, где язык главный.
- **Разделы настроек без хозяина** (из 19а): «Срок сдачи» → 22,
  «Профиль» → 23, «Контакты» → 28.

## Открытые нити

- **Луна в бете на час раньше** (20б) — исправлено 24.09 (`bbfa16f`), Алексей
  считает вопрос закрытым. Пара `fold.moon` теперь должна сойтись —
  пересверит следующий `make shots` карты.
- **Строка загрузки под неделей** (21) — известная ошибка эталона, в натив
  не переносится, веб не правится.
- **Точки «Карты» проверены на телефоне** (24–25.09): ротор по компасу,
  булавки, закладка, тап по булавке с полосой имени — на обеих картах.
  Подставной курс в 21а остаётся — страховка ротора для 29 и 36.
- **Не перенесено из 20б:** подпись «точка убрана / переехали» (`saveNote`),
  кнопка «Мои места» в шапке «Карты» (`mapList`) — решить при 24 (маршрут).
  Список мест есть с 21в (лист «Где снимаем», строка в настройках).
- **Переезд снаружи ставит приближение 14** на обоих холстах (21б записала,
  не правила); веб держит текущее (`jumpTo({ zoom: lmap.getZoom() })`) — 24.
- **Коммит `wip:` в `main`** (`3bfacd8`, 21в) — работа принята, но правило
  «wip — только в ветке»; напоминать исполнителям.
- **Кадр «Карты» на телефоне не мерен** (20а): Mac и симулятор — 0,7–0,9 мс
  на сцену. Мерить на iPhone, только если Алексей заметит торможение при
  движении ползунка.
- **«Музыка» из 19а** — раздел без хозяина, в бете это знак роли.
- **Экспорт с телефона** для 12 — нужен настоящий файл.
- **Украинского языка не будет** (Алексей, 25.09) — `uk` не добавлять.
- **Apple Developer Program** — оплата к 26.09; после — перемерить iCloud,
  Push, TestFlight на платной команде (30, 31, 34).
- Ветки `wt/sun-arc`, `wt/sun-portal` в `Light_Plan` — старые, влиты
  (отстают на 107–108), чужие; удалить может хозяин.
- `ws:CLAUDE.md` — 132 строки при своей норме 125.

## Ждёт Алексея

- файл экспорта с телефона (12);
- оплата Developer Program (к 26.09).
