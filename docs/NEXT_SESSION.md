# Сейчас — состояние миграции для новой сессии

Хозяин — чат дорожной карты (оркестратор): переписывает этот файл после
каждого отчёта итерации. Файл **переписывается, а не дописывается**; держать
до ~100 строк. Решения целиком — `DECISIONS.md`, задания итераций —
`SWIFT_MIGRATION_PLAN.md` § 8, передача между живыми чатами —
`SESSIONS_CHAT.md`. Здесь — то, что нужно знать до начала работы, и то, что
решено в чате дорожной карты. Прежняя версия (веб, 31 августа 2026) — в git.

Обновлено: **24 сентября 2026**.

## Сейчас идёт

- **20б** «Карта: сводка, засветка, слои, ротор, точки» — Opus 5.5, пятый чат.
  Натив — ветка `wt/20b` (worktree `native/.claude/worktrees/it20b`), веб
  `tools/shot.js` — `wt/20b` в `Light_Plan/.claude/worktrees/it20b`.
  Осталось: сохранённые точки, узлы `shot.js`, пары, `xcodebuild test`, коммит.
- **21** «Планировщик: месяц, неделя, день» — Opus 5.5, второй чат. Ядро
  (`PlannerState` — одно состояние на три вида) — `wt/it21` в обоих репо.
  Осталось: вкладка, `AppModel`, три вида, `shot.js`/`pair.js`, пары, коммит.
  Стыки с 20б при слиянии: `AppModel.swift`, `Tools/shots/pair.js`, `tools/shot.js`.
- Основные папки `native/` и `Light_Plan/` чистые — работа только в ветках.
- Лимит (24.09): неделя 44 %, сброс 29.09 18:00 UTC. Итерация на Opus 5.5 с
  чатами до ~300 тыс. — ~6 % недели; чат на ~300 тыс. — ~20 % пятичасового окна.

## Решено в чате дорожной карты — касается ближайших итераций

- **Размер сессии:** у ~300 тыс. токенов — промежуточный итог в план и
  `SESSIONS_CHAT.md`, свежий чат. Повторно не сжимать.
- **Передача — только закоммиченной работы** (24.09): итерация живёт в своём
  worktree и ветке `wt/<задача>`, перед передачей — коммит `wip:` в ветку,
  в `main` — на чекпоинте.
- **Экранная итерация закрывается парой снимков** веб / натив (`make shots`),
  сверенной числами, и места, и цвета. «Не проверено» вместо снимка не
  принимается (§ 5.4). Снимок открывать в контекст только где числа спорят.
- **«Стекло в нативе — системное»** значит: вид прототипа веба на встроенном
  стекле (`.glassEffect`, свой шейдер там, где нужно), **не** системные
  компоненты (не `TabView`, не системные листы вместо наших). 19б прочитала
  иначе и переделывала панель вкладок.
- **Модели:** Opus 5.5 — 20б, 21, 24, 25, 29, 32, 35, 36; остальные Sonnet.
  Замер 20а: цену даёт длина сессии, не модель. Пересмотреть с Sonnet 5.5.
- **Веб заморожен** — эталон. `beta/` не править; `tools/shot.js` —
  инструмент, его учить можно. Ошибку веба, найденную сверкой, записывать, а
  не чинить молча.
- **Непринципиальное** (порядок, фикстуры, куда положить) решает исполнитель
  и пишет в DECISIONS «решение исполнителя»; **продуктовое** (что видит
  человек) — Алексею, 2–3 варианта с выбором исполнителя.
- **Запасная столица** без города и геолокации — страна, где язык главный
  (русский → Москва, Минск).
- **Разделы настроек без хозяина** (из 19а): «Срок сдачи» → 22,
  «Профиль» (мой телефон, ID) → 23, «Контакты» → 28.

## Открытые нити

- **Луна в вебе на час раньше** (найдено 20б): `moonCache` берёт ключ «дата +
  место» без пояса — дуги считаются, пока пояс оценён по долготе (+6), и
  остаются после настоящего (+7). Натив прав. Решить: чинить веб или
  записать как известную ошибку эталона.
- **«Музыка» из 19а** — исполнитель назвал раздел без хозяина, в настройках
  беты его нет (в вебе это знак роли). Выяснить, если всплывёт.
- **Экспорт с телефона** для итерации 12 — нужен настоящий файл
  («Поделиться копией» в настройках беты), импорт проверен синтетикой.
- **Строка «Язык»** в настройках (19а) — открывает ли на телефоне страницу
  Light Plan с пунктом «Язык»; в симуляторе открыла общие «Настройки».
- **Подписи Apple** (13): «Московская Область» привести к вебу после замера
  5–6 областей; Крым / «Украина» — мерить на телефоне Алексея.
- **Apple Developer Program** — Алексей оплатит к 26.09. После — перемерить
  iCloud, Push, TestFlight на платной команде (итерации 30, 31, 34); id
  команды может смениться. До того — сертификат бесплатной команды живёт
  7 дней, сборку на телефон переустанавливать.
- `ws:CLAUDE.md` — 132 строки при своей норме 125.

## Ждёт Алексея

- файл экспорта с телефона (12);
- проверка строки «Язык» и подписи Крыма на телефоне;
- оплата Developer Program (к 26.09).
