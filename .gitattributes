# DECISIONS.md только дописывается в конец. Когда две сессии дописали разборы
# параллельно, git склеивает обе дописки, а не останавливается на конфликте.
# ROADMAP.md сюда не входит: итоги волн правятся в середине файла, и склейка
# задвоила бы текст.
DECISIONS.md merge=union
